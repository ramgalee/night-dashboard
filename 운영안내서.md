# 홀라가람 증시 대시보드 — 운영 안내서

마지막 정리: 2026년 9월 26일 (투자자 동향 자동화 · 순매수 순위 6개 탭 · 메뉴 통일 · 미국 ETF 자금 흐름) · 주소 https://holagaram.com

코딩을 몰라도 매일 운영하고, 문제가 생겼을 때 원인을 찾을 수 있도록 쓴 문서입니다.
새 대화에서 작업을 이어갈 때 이 문서를 먼저 올리면 구조를 바로 파악할 수 있습니다.

---

## 0. 9월 26일에 바뀐 것 (요약)

| 무엇 | 전 | 후 |
|---|---|---|
| 티스토리 투자자 동향 표·차트 | 엑셀 5개를 매일 손으로 | **브리핑 페이지가 자동으로** (키움 + 네이버) |
| 순매수 순위 | 외국인 · 기관 2개 탭 | **+ 금융투자 · 투신 · 연기금 · 사모펀드** 6개 탭 |
| 윗단 메뉴 | 페이지마다 달랐음(4~12개) | **`menu.js` 하나로 13개 통일** |
| 미국 증시 | 업종·ETF 자금 흐름 없음 | **업종 11개 + ETF 자금 흐름 + 미국 ETF의 한국 매매** |
| 서버 모듈 | 17개 | **21개** (`us_flow` · `kr_etf_flow` · `index_daily` · `fut_flow` 추가) |
| 주피터 | 칸 번호가 제각각 | **칸마다 맨 윗줄에 【이름표】**, 맨 위에 매일 순서 목차 |

---

## 1. 지금 밀려 있는 일

**순매수 순위 새 탭 4개 채우기** — 칸 32 가 한 번에 20일치씩 받습니다(로그인 정지 막기).
180일이 다 차려면 매일 한 번씩 돌려 **약 9번**. 40일이 쌓이기 전에는 ★ 표시 없이 비율 순서만 나옵니다.

**증시 일정 `calendar.json`** — 칸 26 이 본보기 `증시일정.csv` 만 만들어 둔 상태입니다.
국내 일정을 채워 칸 26 을 다시 돌리고 `calendar.json` 을 올려야 달력에 국내 일정이 나옵니다
(미국 지표는 미국 노동부 달력에서 알아서 들어옵니다).

**DRAM · SMH · QQQ 자금 흐름이 '추정'** — 운용사 공시를 못 받아 야후 순자산으로 추정합니다.
야후 순자산은 며칠씩 갱신이 안 될 때가 많아 `순자산 미갱신` 이 자주 뜹니다.
인베스코(QQQ) · 반에크(SMH) · 라운드힐(DRAM) 공시를 직접 받으면 정확해집니다(할 일).

**브리핑 "그림으로 저장" 버튼 시험** — 티스토리용 그림 저장. 아직 실제로 눌러 보지 못했습니다.

**차트 창 축 맞추기** — 주간 증시 · 순매수 순위의 종목 차트 창에서 캔들과 오실레이터 축이 조금 어긋납니다.

**저장소 정리** — 잘못 올라간 파일 셋. 지워도 되고 둬도 해는 없습니다.
`api/brief.html` · `api/us.html` (html 은 맨 바깥에만), 맨 바깥의 `infra-theme.js` (진짜는 `api/infra-theme.js`).

**테마 히트맵의 종목 커버리지** — 업종을 돌아 모으는 방식이라 2,100~2,400종목만 잡힙니다
(상장 2,700 중). 빠지는 것은 대부분 작은 종목이고 거래대금 가중이라 영향은 작습니다.

**ADR 코스닥 정확도** — 시장 구분을 모르는 종목 때문에 adrinfo 보다 2~3 높게 나옵니다.
KRX 가 풀린 날 칸 25 를 돌리면 `market_map.json` 이 만들어져 해결됩니다.

**`fg_extra.json` 을 KRX 로 완전히 갈아타기** — 지금은 엑셀 우선, 없으면 KRX(6-3 참고). 당분간 유지.

**안 하기로 한 것** — 시가대비 등락률(테마 히트맵이 대신함), RS 상위 탭 시총 거르기(대형주 수급 페이지를 씀).

---

## 2. 매일 할 일

### 아침 (장 시작 전)

만들 것은 없습니다. 서버가 알아서 돌아갑니다. 보기만 하면 됩니다.

1. `Ctrl+Shift+R` 로 holagaram.com 열기 — 지수·수급에 숫자가 차 있는지
2. **야간 증시** · **미국 증시**(업종·ETF 자금 흐름 · 텍스트 복사) — 밤사이 결과
3. **오늘의 브리핑** — 어제 마감 요약 · 3거래일 연속 순매수 · **투자자 동향 표·차트**

자세한 것은 `운영_체크리스트.html`(인쇄용)을 보세요. (9/26 변경은 아직 반영 전)

### 장 마감 후 (16시 이후)

**장중에는 돌리지 마세요.** 종가 자리에 현재가가 저장되면 그 날짜는 채워진 것으로 간주돼
다시는 받아오지 않습니다. 잘못된 값이 영구히 남습니다.

1. HTS에서 `피어앤그리드.xlsx` 새로고침 (없어도 칸 12가 KRX로 채웁니다)
2. `G:\내 드라이브\주식데이터`에서 주피터 실행 (`py -m jupyter lab`)
3. **`KRX_수급비율_Colab__1_.ipynb`** 를 엽니다
4. 맨 위 **📋 매일 순서** 표를 보며, 칸마다 맨 윗줄의 **【이름표】** 로 찾아 순서대로 실행합니다

```
준비  칸 4    KRX 로그인                        (주피터를 켤 때마다 맨 먼저)
준비  칸 6    설정 · 지금 폴더 파일 현황
 ①   칸 8    전 종목 투자자별 수급 받기          (시간 걸림)
 ②   칸 10   supply_final.json + supply_history.json
 ③   칸 12   fg_extra.json
 ④   칸 9    marketcap.json
 ⑤   칸 16   rs.json + price_cache.csv
 ⑥   칸 21   쏠림지수 ① 코스피 업종 받기
 ⑦   칸 22   쏠림지수 ② sector_tilt.json      ★ 엑셀을 닫고
 ⑧   칸 24   업종 수급 오실레이터 (180일 자료를 칸 32 가 씀)
 ⑨   칸 28   etf_flow.json (액티브 ETF)
 ⑩   칸 36   signal.json (시장 신호등)
 ⑪   칸 32   supply_rank.json (순매수 순위)    처음 9일쯤은 5분, 그 뒤 20초
```

**칸 번호는 셀 맨 윗줄 이름표에 적힌 번호입니다.** 위에서 몇 번째 칸인지와는 다를 수 있습니다.
(주피터의 `[12]` 같은 실행 번호와도 다릅니다.)

5. **json 아홉 개**를 GitHub 저장소 **맨 바깥**에 업로드

```
marketcap.json        시가총액 + 종목명·시장 → 시총대비 수급, 히트맵, 프로그램, 대형주 수급
supply_final.json     확정 수급             → 마감 확정 패널, 양매수 탭, 엑셀 사모 목록
supply_history.json   수급 이력(최근 12거래일) → 브리핑의 3거래일 연속 순매수
fg_extra.json         VKOSPI·Put/Call·국채   → F&G 지수
rs.json               RS·신고가·정배열·등락률 → 히트맵 아래 목록, 수급오실레이터 거르기
sector_tilt.json      업종 쏠림지수 + 종목    → 공포·탐욕 지수 맨 아래 카드
etf_flow.json         액티브 ETF 편입·편출    → 액티브 ETF 페이지
signal.json           시장 신호등            → 시장 신호등 페이지
supply_rank.json      시총대비 순매수 순위    → 순매수 순위 페이지 (6개 탭)
```

**`flow_diary.json` 은 이제 매일 올리지 않습니다.** 브리핑의 투자자 동향은 서버가 채웁니다(4번 참고).
이 파일은 서버가 멈췄을 때 쓰는 비상용 씨앗이라 그대로 두면 됩니다.

`theme_map.json` 은 **엑셀을 고친 날만** 올립니다. `calendar.json` 은 **증시일정을 고친 날만** 올립니다.
`supply_history_bak.json` · `sector_tilt_bak.json` · `price_cache.csv` · `*.gz` · `테마맵.xlsx` 는
올리지 않습니다. 주피터 폴더에만 둡니다.

### 2-2. 칸 의존 관계 — 순서를 지켜야 하는 이유

```
칸 8   ← DATE (칸 6)
칸 10  ← INVESTORS · MIN_MARKET_CAP (칸 6) · data · target (칸 8)
칸 22  ← 칸 21 이 받은 코스피 업종 자료
칸 36  ← price_cache.csv (칸 16)
칸 32  ← sector_flow_cache.json.gz (칸 24) · KRX 로그인 (칸 4)
칸 24 · 28 · 32 · 36 ← KRX 로그인 (칸 4)

나머지 칸은 앞 칸이 필요 없어 언제든 혼자 돌릴 수 있습니다
```

주피터를 껐다 켜면 기억이 사라지므로 **항상 칸 4부터** 시작합니다.
`target is not defined` = 칸 8을 안 돌린 것, `DATE is not defined` = 칸 6을 안 돌린 것입니다.

### 2-3. 칸별 실행 빈도

```
매일      칸 4 · 6 · 8 · 10 · 12 · 9 · 16 · 21 · 22 · 24 · 28 · 36 · 32
가끔      칸 17 (수급오실레이터 엑셀) · 칸 26 (증시 일정)
          칸 25 (ADR 이력 — 서버가 매일 쌓으므로 복구할 때만)
          칸 20 (wi_map.json — 업종 분류가 바뀌었을 때)
거의 안 씀  칸 11 (index_history — 1년에 한두 번)
확인용     칸 14 (엑셀 통계) · 【확인용】 폴더 쓰기 시험
막아둠     【쓰지 않음】 4개 · 칸 31 · 칸 35 — 누르면 빨간 글씨로 멈춥니다
```

### 2-4. 칸 32 (순매수 순위) — KRX 로그인 정지 막기

금융투자·투신·연기금·사모펀드는 칸 32 가 KRX 에서 직접 받아 `investor_detail_cache.json.gz` 에 쌓습니다.

```
한번에최대 = 20     한 번에 받을 최대 날수 (160번 호출 · 5분). 불안하면 10 으로
최소견줄날 = 40     쌓인 날이 이보다 적으면 ★ 표시를 하지 않음
최소시총   = 1000억  이보다 작은 종목은 뺌
```

2일 연속 실패하면 스스로 멈추고 받은 것까지만 씁니다. 그때는 몇 시간 뒤 다시 돌리세요.
외국인·기관 탭은 칸 24 자료를 쓰므로 KRX 를 더 부르지 않습니다.

### 매주 일요일 — 비밀번호 변경

1. Vercel → 프로젝트 → `Settings` → `Environments` → `Production`
2. `SITE_PASSWORD` 오른쪽 ⋯ → `Edit` → 새 비밀번호 → 저장
3. `Deployments` → 맨 위 배포 ⋯ → `Redeploy`

재배포까지 해야 적용됩니다. 값은 Secret이라 다시 볼 수 없으니 따로 기록해 두세요.

---

## 3. 전체 구조

```
[데이터 출처]              [Vultr 중계 서버]      [Vercel]          [브라우저]
키움 REST API        →    141.164.40.229   →   api/ 함수들   →   holagaram.com
네이버 증권(선물 수급)       (고정 IP · 매일
iShares · State Street      쌓아야 하는 것)
야후 파이낸스
대만거래소·CNBC·BLS   ─────────────────────→
```

**Vultr가 필요한 이유** — 키움 API는 등록된 고정 IP에서만 호출을 받습니다.
하루치만 주는 자료(네이버 선물, 야후 순자산, iShares 보유내역)를 날마다 쌓아 두는 곳이기도 합니다.

**Vercel 함수가 필요한 이유** — https 페이지에서 http 서버를 직접 못 부르고(혼합 콘텐츠),
외부 사이트도 브라우저가 직접 부르면 막힙니다(CORS).

**키움 REST 에는 선물·옵션 자료가 없습니다.** 국내주식·미국주식·환전뿐입니다.
그래서 코스피200 선물 투자자별 순매수는 네이버 증권 모바일 자료를 씁니다(`fut_flow.js`).

### 3-2. 노트북이 두 개로 갈라져 있습니다 ★

구글 드라이브 동기화 충돌로 한 노트북이 둘로 쪼개졌습니다. **합치지 않은 상태입니다.**

| | `KRX_수급비율_Colab__1_.ipynb` | `KRX_수급비율_Colab.ipynb` |
|---|---|---|
| 매일 쓰는 칸 전부 | ✅ | 일부 |
| 거래대금 / 주간동향 / `데이터.xlsx` | ❌ | ✅ |

**매일 쓰는 건 `__1_` 쪽입니다.** 거래대금·주간동향·데이터가 필요할 때만 다른 쪽을 엽니다.
(투자자 동향 표는 이제 브리핑 페이지가 대신합니다.)

어느 쪽인지 헷갈리면 맨 위에 **📋 매일 순서** 표가 있는지 보세요. 있으면 `__1_` 입니다.
**두 컴퓨터에서 동시에 같은 노트북을 열지 마세요.**

---

## 4. 페이지 열세 개

| 페이지 | 주소 | 내용 |
|---|---|---|
| **오늘의 브리핑** | `/brief.html` | AI 시황 요약 + 오늘의 흐름(장중 30분마다) + 3거래일 연속 순매수 + **투자자 동향 표·차트** · 텍스트 복사 |
| 주간 증시 모니터 | `/` (`/index.html`) | 국내 지수, 아시아·참고 지표, 시장 폭(ADR·거래대금 비중), 거래대금 상위, 장중/확정 수급(+양매수), 순매수·순매도 상위 |
| 시장 신호등 | `/signal.html` | 5년치로 검증한 신호(경계·강세·과열·바닥) + 지금 장 설명 · `signal.json` (칸 36) |
| 증시 일정 | `/calendar.html` | 미국 지표(BLS 공식 달력, 자동) + 국내 일정(`calendar.json`, 칸 26) |
| **순매수 순위** | `/supply-rank.html` | 시총대비 순매수 순위 · **6개 탭** · ★ = 그 종목 180일 중 몇 번째로 큰 날 |
| 수급오실레이터 | `/flow-oscillator.html` | 오늘의 주도주 · 종목 캔들 + 수급 MACD, 탭 5개 + 거르기 |
| 대형주 수급 | `/leader.html` | 시총 상위 30종목의 20일 시총대비 수급 + 오실레이터 |
| 액티브 ETF | `/etf-flow.html` | 국내 주식형 액티브 ETF 106개의 편입·편출(주식수 기준) · `etf_flow.json` (칸 28) |
| 국내 히트맵 | `/map.html` | 업종 히트맵 → 업종 F&G·MACD → 종목 히트맵 · 테마 히트맵 · 신고가·정배열 |
| 공포·탐욕 지수 | `/fg.html` | 코스피·코스닥 F&G + AI 해석 · 업종 쏠림지수 + AI 해석 |
| 야간 증시 모니터 | `/night.html` | 코스피200 야간, SK하이닉스·삼성전자 추정가, 미국 지표 |
| **미국 증시** | `/us.html` | 간밤의 흐름 · AI 요약 · 129종목 히트맵 · **업종 11개 · ETF 자금 흐름 · 미국 ETF의 한국 매매** |
| 연휴 정리 | `/holiday-brief.html` | 추석 연휴 미국 증시 3일 총정리 (연휴 때만 쓰는 페이지) |

### 4-1. 윗단 메뉴 — `menu.js` ★

모든 페이지의 메뉴는 **`menu.js` 한 파일**이 그립니다. 어느 페이지에서나 같은 13개가 같은 순서로 나오고,
지금 보고 있는 페이지는 연보라색 **●** 로 표시됩니다. 버튼이 많으면 다음 줄로 내려갑니다.

**새 페이지를 만들면 `menu.js` 의 `메뉴` 목록에 한 줄만 넣고 GitHub 맨 바깥에 올리면**
모든 페이지 메뉴에 한꺼번에 생깁니다. 페이지 쪽에는 메뉴 자리에 이 한 줄만 있으면 됩니다.

```html
<span id="hgMenu" style="display:contents"></span><script src="/menu.js"></script>
```

`menu.js` 는 브라우저가 읽는 파일이라 **서버에 받을 필요가 없고**, `api` 폴더에 넣으면 안 됩니다.

### 4-2. 브리핑 — 투자자 동향 (티스토리 캡처와 같은 표·차트) ★

예전에 엑셀 5개로 매일 만들어 티스토리에 올리던 두 장을 그대로 옮겼습니다. **매일 할 일이 없습니다.**

```
첫째 장   코스피 5일 표 · 주간 외국인/기관 막대 · 코스닥 5일 표 · 막대
둘째 장   선물 표 + 외국인 누적 · 곱버스/삼성전자/SK하이닉스 5일 표
          4월이후 개인 곱버스 & 코스피 · 삼성전자 & 개인 누적 · 코스피 외국인 누적
```

| 무엇 | 어디서 | 단위 |
|---|---|---|
| 코스피·코스닥 투자자별 (개인~사모펀드) | 서버 `/flow-history` · 키움 ka10051 | 억원 |
| 코스피·코스닥 종가·거래대금 | 서버 `/index-daily` · 키움 ka20006 (`index_daily.js`) | 억원 |
| 코스피200 선물 개인·외국인·기관 | 서버 `/fut-flow` · 네이버 증권 (`fut_flow.js`) | 억원 |
| 삼성전자 · SK하이닉스 · KODEX 200선물인버스2X | 서버 `/stock-flow` · 키움 ka10059 | 백만원 |
| 서버에 없는 옛날 | `flow_diary.json` (엑셀 씨앗, ~2026-09-22) | |

`api/flow-diary.js` 가 이것들을 모아 화면 모양으로 묶습니다(10분 저장).
표 아래 출처가 `키움 시장 · 키움 거래대금 · 네이버 선물 · 키움 종목` 이면 모두 정상입니다.

- **자사주 칸 = 거래소 분류의 기타법인**입니다(회사의 자사주 매입이 여기에 잡힘).
- **휴장일·주말 줄은 뺍니다.** 서버 자료에 전날 숫자가 반복돼 들어 있어 그대로 두면 주간 합이 부풀려집니다.
- **주간 막대** — 월~금 한 주, 이름은 그 주 목요일이 속한 달의 몇째 주("4월1").
- 2026-09-26 검증 — 코스피·코스닥 5일 × 10칸 **100칸 모두 엑셀과 같음**, 선물 외국인 **26일 모두 같음**.
- **그림으로 저장** 버튼 — 카드 한 장을 그림 파일로 받습니다(티스토리용).

### 4-3. 순매수 순위 — 6개 탭

```
외국인 · 기관                   칸 24 의 180일 자료 (KRX 를 더 안 부름)
금융투자 · 투신 · 연기금 · 사모펀드  칸 32 가 KRX 에서 받아 쌓음
```

기관 = 금융투자 + 보험 + 투신 + 사모 + 은행 + 기타금융 + 연기금이므로, 한 종목의 투신·사모 금액이
기관 금액 안에 들어가는 것이 정상입니다. **금융투자**는 증권사 헤지·유동성 공급 물량이 많아 종목 수는 많고
금액은 작습니다(10억 미만은 6.8억처럼 소수 한 자리). 탭마다 오른쪽 위에 "최근 ○일과 견줌"이 나옵니다.

### 4-4. 미국 증시 — 업종 · ETF 자금 흐름

**업종 11개**(XLK~XLRE) 등락과, State Street 공시로 계산한 **업종별 하루·5일·20일 자금**.

**ETF 자금 흐름** — `정확` 과 `추정` 두 가지입니다.

```
정확   공시 상장주식수 변화 × NAV         EWY · EEM · IEMG (iShares) · SPY · XLK (State Street)
추정   순자산 − 어제 순자산 × 가격 변화     DRAM · SMH · QQQ (야후 순자산)
```

추정은 서버가 매일 적어 둔 순자산끼리 비교하므로 **처음 이틀은 "첫날"** 이 나옵니다.
야후 순자산이 안 바뀐 날은 `순자산 미갱신`, 하루 15% 넘게 튀면 `값이 튐 — 참고만` 으로 가립니다.

**미국 ETF의 한국 매매** (EWY · IEMG · EEM) — iShares 가 매일 공시하는 보유내역을 전날과 견줘
한국 종목을 얼마나 사고팔았는지 계산합니다. **현물**(보유 주식 수 변화 × 가격)과 **자금**(설정·환매)이
따로 나옵니다. 둘이 다른 날은 지수 비중 조정(EWY 25/50 상한) 같은 운용상 매매입니다.
한국 휴장일에는 0원으로 나오는 것이 정상입니다(추석 9/24~25).

### 양매수 탭 · 연속 순매수 · 수급오실레이터 탭 · 테마 히트맵 · 쏠림지수 · 대형주 수급

9월 24일판과 같습니다. 요점만:

- **양매수** — 외국인 합과 기관합계 상위 목록에 **둘 다 든** 종목. 안내 문구 `각 ○위 이내 겹친 ○종목`.
- **3거래일 연속 순매수** — `supply_history.json` 에서 각 창구 상위 50에 사흘 내리 든 종목.
- **수급오실레이터 탭 5개** — 상승률 50 · 프로그램 50 · 잠정 기관 · 사모 20 · RS 상위. 거르기 4개(신고가·정배열).
- **테마 히트맵** — 분류는 `theme_map.json`(키움 HTS [0659]), 숫자는 키움 업종 시세의 거래대금 가중 평균.
  테마를 더하려면 `테마맵.xlsx` 에 시트 추가 → `theme_map.json` 만들기 → GitHub 과 서버 양쪽에 올림.
- **업종 쏠림지수** — WI26 26개 업종의 (상위 5 − 나머지) ÷ 업종 수를 누적해 MACD. 코스피는 wiseindex,
  코스닥은 `특정업종 쏠림지수 국내.xlsx`. 동조화 = 업종끼리 30일 상관의 평균.
- **대형주 수급** — "수급 강한 순"은 누적 총량이 아니라 **오실레이터 마지막 값**(최근 빨라진 종목이 위로).

```
theme_map.json 을 서버에도 받기
cd /root/app && curl -s -o theme_map.json https://raw.githubusercontent.com/ramgalee/night-dashboard/main/theme_map.json && pm2 restart all
```

---

## 5. 서버가 하는 일 (Vultr `/root/app/`)

| 파일 | 하는 일 | 출처 |
|---|---|---|
| `server.js` | 중계 서버 본체 (3000번 포트) + 수급오실레이터 | 키움 |
| `intraday.js` | 국내 지수·투자자 매매동향·종목 시세 | ka20005, ka20001, ka10051, ka10001 |
| `supply.js` | 장중 잠정 시총대비 수급 | ka10065 |
| `trade_rank.js` | 거래대금 상위 150 · 상승률 50 | ka10032 |
| `program_flow.js` | 시총대비 프로그램 순매수 | ka90003 |
| `sector_map.js` | 업종 히트맵 · 업종 종목 · 업종 F&G/MACD | ka20003, ka20002, ka20006 |
| `fear_greed.js` | 코스피·코스닥 F&G | ka20006 + fg_extra.json |
| `flow_history.js` | 일별 시장 투자자별 수급 (`/flow-history`) | ka10051 |
| `stock_flow.js` | 종목별 일별 투자자 수급 (`/stock-flow`) | ka10059 |
| **`index_daily.js`** | 코스피·코스닥 일별 종가·**거래대금** (`/index-daily`) | ka20006 |
| **`fut_flow.js`** | 코스피200 **선물** 투자자별 — 30분마다 쌓음 (`/fut-flow`) | 네이버 증권 |
| `us_stocks.js` | 미국 129종목 등락률 | 야후 |
| **`us_flow.js`** | 미국 업종 · ETF 자금 흐름 · State Street 이력 (`/us-sectors` `/us-flow`) | 야후 · iShares · State Street |
| **`kr_etf_flow.js`** | 미국 iShares ETF 의 한국 종목 매매 — 3시간마다 (`/kr-etf-flow`) | iShares 보유내역 |
| `krx_investor_rank.js` | 순매수 상위 10 — 평일 18:30 크론 | ka90009 |
| `leader_flow.js` | 대형주 수급 — 3시간마다 혼자 수집 | ka10059 |
| `leader_scan.js` | 오늘의 주도주 — 후보 50종목을 5분마다 | (내부 호출) |
| `market_breadth.js` | 시장 폭 — ADR·거래대금 비중·신고가 | (내부 호출 + rs.json) |
| `infra_theme.js` · `theme_map.js` | 테마 히트맵 | theme_map.json + 업종 시세 / ka90001 |
| `timeline_feed.js` | 국내·미국 타임라인 — 30분마다 AI 문장 | (내부 호출 + Claude) |
| `portfolio_api.js` | 모의 포트폴리오 (`/pf/…`) | ka10001 |
| `bls_feed.js` | 미국 노동부 달력 중계 (`/bls-ics`) | bls.gov |

`server.js` 에 `require('./파일')(app);` 줄이 **21개** 있어야 정상입니다(2026-09-26 기준).
목록은 이 명령으로 봅니다.

```
grep "require('./" /root/app/server.js
```

### 서버가 읽고 쓰는 파일 — ★ 표시는 지우지 마세요

```
/root/app/.env                     키움 키 · ANTHROPIC_API_KEY
/root/app/breadth_history.json     ADR 이력 · 매일 한 줄씩 ★
/root/app/portfolio.json           모의 포트폴리오 ★
/root/app/fut_flow.json            선물 투자자별 (6/25~ · 30분마다) ★ 네이버는 과거를 다시 줄 때만 복구됨
/root/app/us_flow_history.json     미국 ETF 순자산 기록 (추정 계산용) ★
/root/app/kr_etf_history.json      iShares 보유내역 (EWY·IEMG·EEM 130일+) — 지우면 몇 시간 걸려 다시 받음
/root/app/ssga_nav.json            State Street 400일 — 지워도 6시간 안에 다시 받음
/root/app/theme_map.json           테마 분류 (엑셀 고친 날 받아감)
/root/app/krx_index_members.json   KRX 지수 구성종목 · 주 1회
/root/app/leader_flow.json         대형주 수급 (3시간마다 새로)
/root/app/timeline/ · timeline_us/ 타임라인 · 30일 보관
```

### AI 기능 (Claude API)

| 기능 | 어디 | 언제 |
|---|---|---|
| 시황 요약 | 브리핑 · 미국 증시 | 화면을 열 때 (20분 저장) |
| 지표 해석 | F&G · 업종 쏠림지수 | 화면을 열 때 |
| 국내 타임라인 | 브리핑 "오늘의 흐름" | 평일 09:00~15:40 · 30분마다 |
| 미국 타임라인 | 미국 증시 "간밤의 흐름" | 미국 장중 30분마다 + 마감 정리 |

서버가 한 번 만들어 회원 모두가 같은 글을 봅니다. 하루 0.5~1달러 예상.
**잔액이 0 이 되면 요약과 타임라인만 멈추고** 나머지 화면은 그대로 돕니다.
요약 규칙 — 주어진 수치·뉴스 제목에 없는 원인은 지어내지 않기, 투자 권유·전망 금지, `AI 작성` 표시.

**중요한 규칙** — 종목·업종 코드 뒤에 `_AL`을 붙여야 넥스트레이드까지 합친 통합 수치가 나옵니다.

### 서버 파일 올리는 법

파일 이름에 **밑줄(`_`)** 이 있으면 서버 파일, **하이픈(`-`)** 이면 Vercel 함수(`api` 폴더)입니다.

```
1) GitHub 맨 바깥에 올림
2) 서버에서 받고 문법 확인
cd /root/app && curl -s -o 파일.js https://raw.githubusercontent.com/ramgalee/night-dashboard/main/파일.js && node --check 파일.js && echo 문법OK
3) 새 파일이면 server.js 에 한 줄 넣기 (백업 후 자동으로 app.listen 위에)
cp server.js server.js.bak_$(date +%m%d%H%M) && node -e "const fs=require('fs');let s=fs.readFileSync('server.js','utf8');const L=\"require('./파일')(app);\";if(s.includes(L)){console.log('이미 있음');process.exit(0)}const i=s.lastIndexOf('app.listen');if(i<0){console.log('app.listen 못찾음');process.exit(1)}const j=s.lastIndexOf('\n',i)+1;s=s.slice(0,j)+L+'\n'+s.slice(j);fs.writeFileSync('server.js',s);console.log('넣음')" && node --check server.js && echo 문법OK
4) pm2 restart all
```

- GitHub Raw 는 올린 뒤 **몇 분 동안 옛 파일을 줄 때**가 있습니다. 바뀐 게 안 보이면 2~3분 뒤 다시 받으세요.
- `node --check` 에서 `Unexpected token ':'` = GitHub 에 파일이 없어 `404: Not Found` 글자가 저장된 것.
- **명령에 `!` 를 넣지 마세요.** 서버 터미널이 `!` 를 "예전 명령 불러오기"로 읽어 명령이 깨집니다
  (`SyntaxError: Unexpected number`).
- **서버 명령은 반드시 SSH 로 들어간 뒤** 칩니다(`root@kiwoom-relay:~/app#` 가 보여야 함).
  윈도우 창(`C:\Users\...>`)에서 치면 아무 일도 안 일어납니다.

---

## 6. 파일이 어디에 있나

### GitHub 저장소 (= Vercel 배포)

```
화면 13개   index · brief · signal · calendar · supply-rank · flow-oscillator · leader
            etf-flow · map · fg · night · us · holiday-brief  (.html)
menu.js                 모든 페이지 메뉴 ★
middleware.js           비밀번호 보호
운영안내서.md · 운영_체크리스트.html

매일 교체   marketcap · supply_final · supply_history · fg_extra · rs · sector_tilt
            etf_flow · signal · supply_rank  (.json)
가끔 교체   theme_map.json (테마 엑셀 고칠 때) · calendar.json (증시일정 고칠 때)
            breadth_history.json (칸 25 로 복구할 때)
씨앗        index_history.json · flow_diary.json · us_calendar.json  (지우지 말 것)

서버로 옮기는 통로 (밑줄 이름)
            us_flow.js · kr_etf_flow.js · index_daily.js · fut_flow.js · leader_flow.js · leader_scan.js
            market_breadth.js · sector_map.js · infra_theme.js · theme_map.js · timeline_feed.js
            portfolio_api.js · bls_feed.js
api/        Vercel 함수들
```

`api/` — `calendar` `fear-greed` `flow-diary` `flow-oscillator` `index-intraday` `infra-theme`
`investor-rank` `kr-etf-flow` `kr200` `leader-flow` `leader-scan` `macro` `macro-spark` `market`
`market-breadth` `program-flow` `sector-indicator` `sector-map` `sector-stocks` `summary`
`supply-ratio` `telegram-news` `theme-map` `timeline` `trade-rank` `twse` `us-flow` `us-news`
`us-sectors` `us-stocks` (.js)

Vercel 함수는 전부 `export default async function handler(req, res)` 형식입니다.
`module.exports` 방식으로 쓰지 마세요. Pro 플랜이라 함수 개수 제한은 없습니다.

### 주피터 (`G:\내 드라이브\주식데이터`)

| 노트북 | 하는 일 |
|---|---|
| `KRX_수급비율_Colab__1_` | **매일 쓰는 것.** 2번의 칸 13개 |
| `KRX_수급비율_Colab` | 거래대금, 주간동향, 데이터 |
| `program_realtime` | 프로그램 수급 CSV → 엑셀 파워쿼리 |
| `장중잠정_시총대비수급` | 서버가 대신하므로 안 써도 됨 |

### 6-2. 쌓아가는 파일 — 절대 지우지 마세요 ★

주피터 폴더

```
price_cache.csv                110거래일 종가 — 지우면 하루 40일씩 사흘 걸려 복구
supply_history.json            수급 이력 — 지우면 3거래일 다시 모아야 함
supply_history_bak.json        그 백업 (칸 10이 매번 함께 저장)
fg_extra.json                  과거치 누적 (칸 12가 기존 것을 읽어 이어 붙임)
sector_tilt_bak.json           쏠림지수 백업
index_history.json             지수 씨앗
sector_flow_cache.json.gz      전 종목 180일 (시총·외국인·기관) — 칸 24 · 32 가 씀. 지우면 25분 복구
investor_detail_cache.json.gz  금융투자·투신·연기금·사모 — 칸 32. 지우면 9일에 걸쳐 다시 받음 ★
etf_pdf_cache.json.gz          액티브 ETF 날짜별 구성 — 칸 28. 지우면 어제와 비교를 못 함 ★
breadth_history.json           ADR 이력 — 칸 25 로 복구되지만 서버 것과 합쳐 쓰므로 주의
market_map.json                전 종목 시장 구분표
wi_map.json                    WI26 업종 분류 (칸 20)
krx_index_members.json         KRX 지수 구성종목 (칸 24 가 주 1회)
```

**매일 새로 만들어도 되는 것** — `marketcap.json` · `supply_final.json` · `rs.json` · `sector_tilt.json` ·
`signal.json` · `supply_rank.json`. 나머지는 이어 붙이는 파일입니다.

`supply_history.json`이 사라져도 `supply_history_bak.json`에서 자동으로 되살아납니다.
칸 10이 실행될 때마다 `[수급이력] 본 파일 ○일 · 백업 ○일` 이 찍히므로 그 줄만 보면 됩니다.

### 6-3. fg_extra.json 의 두 갈래 (칸 12)

```
1) 피어앤그리드.xlsx 를 읽어 채웁니다          — 당일 값
2) 엑셀이 없거나 날짜가 모자라면 KRX 로 채웁니다 — 전일까지만
```

| 항목 | KRX API | 찾는 값 |
|---|---|---|
| `vkospi` | `/idx/drvprod_dd_trd` | `코스피 200 변동성지수` 의 `CLSPRC_IDX` |
| `bondDiff` | 같은 API | `10년국채선물지수` − `5년 국채선물 추종 지수` |
| `putCall` | `/drv/opt_bydd_trd` | 코스피200 옵션 콜·풋 전체 거래량(정규+야간) 5일 이동평균의 풋÷콜 |

엑셀의 `최근월물 CALL/PUT ATM` 열은 이름과 달리 **콜·풋 전체 거래량의 5일 이동평균**입니다.

### sector_tilt.json · rs.json 구조

```
sector_tilt  generatedAt, topN, corrWindow, source, sectors{KOSPI,KOSDAQ}
             data{KOSPI:[{date, tilt, index, corr, top[5], macd, signal, osc}], KOSDAQ}
             stocks{KOSPI:{업종명:[{code,name,cap,chg,rs,high60,aligned}×10]}}, stockDate
rs           date, generatedAt, count, minCap, bars, cacheLast, trendValid
             data{종목코드:{name, market, close, chg, rs, m3, m6, m9, m12, high60, aligned}}
supply_rank  date, days, daysBy{투자자:견준날수}, topRank, minCap(억), investors[6]
             data{투자자:[{code,name,ratio,amount(억),cap(조),rank,days,todayRank}×40]}
```

---

## 7. 문제가 생겼을 때

### 먼저 `Ctrl + Shift + R`

"분명 올렸는데 화면이 그대로"의 대부분은 브라우저 캐시입니다.

### 어디까지 정상인지 한 번에 가르기

서버 → Vercel → 브라우저 순서로 같은 것을 봅니다.

```
curl -s localhost:3000/fear-greed?days=3 | tail -c 300      ← 서버 (SSH 에서)
https://holagaram.com/api/fear-greed?days=3                 ← Vercel (브라우저에서)
```

서버는 맞는데 Vercel이 다르면 Vercel 캐시(대개 10분 안에 풀림), 둘 다 맞는데 화면이 다르면 브라우저 캐시.
`404 NOT_FOUND` → 파일이 `api` 폴더 밖에 있거나 이름이 틀림.

### 메뉴가 안 보이거나 일부 페이지만 옛 메뉴일 때

`holagaram.com/menu.js` 를 열어 `menu.js — 모든 페이지 윗단 메뉴를…` 이 보이는지 봅니다.
안 보이면 맨 바깥에 없는 것. 한 페이지만 옛 메뉴면 그 html 이 옛날 것입니다(새 것으로 다시 올리기).

### 브리핑 투자자 동향이 비거나 날짜가 안 늘 때

```
https://holagaram.com/api/flow-diary?t=1           ← 맨 아래 source · serverLast 확인
curl -s "localhost:3000/index-daily/probe?code=001"  ← 키움 거래대금 (keys 에 trde_prica)
curl -s "localhost:3000/fut-flow/refresh"            ← 선물 (lastRow · error)
```

- `source` 에 `선물은 엑셀` 이 붙어 있으면 서버 선물 자료를 못 받은 것 → `fut-flow/refresh` 의 `error` 를 봅니다.
- 선물이 `모양이 바뀜` 이면 네이버가 주소나 형식을 바꾼 것입니다. 쌓인 날은 남아 있고 새 날만 멈춥니다.
- 엑셀 표와 맞춰 보는 검증 스크립트: `node /root/app/chk_fd.js` (9/17~9/23 100칸 비교).

### 순매수 순위 새 탭이 안 나올 때

`holagaram.com/supply_rank.json` 의 `investors` 에 6개가 있는지 봅니다.
2개뿐이면 칸 32 가 오늘치 금융투자 등을 못 받은 것입니다(로그인 정지 → 몇 시간 뒤 다시).

### 미국 ETF 자금 흐름

```
curl -s localhost:3000/us-flow/ssga       State Street 12개 모였는지 (됨 12)
curl -s localhost:3000/kr-etf-flow/status iShares 3개 며칠 모였나
```

- `첫날 — 다음 거래일부터` → 추정 ETF 는 서버에 이틀치가 쌓여야 계산됩니다. 정상.
- 서버를 켜고 1분 안에 보면 State Street 가 `"마지막":null` 입니다. 1분 뒤 다시.

### 수급오실레이터 차트가 안 나올 때 · 여러 기능이 한꺼번에 500 일 때

대개 키움 토큰 문제이고 `pm2 restart all` 로 풀립니다.
키움을 안 쓰는 기능(`us-stocks`, `fear-greed`)이 정상이면 키움 문제입니다.

```
for p in index-intraday fear-greed us-stocks trade-rank; do echo -n "$p: "; curl -s -o /dev/null -w "%{http_code}\n" localhost:3000/$p; done
```

### 대형주 수급이 비어 있을 때

```
curl -s localhost:3000/leader-flow | head -c 200
pm2 logs --lines 40 --nostream | grep leader_flow
```

`info(종목명·시장)가 없습니다` → `marketcap.json` 이 옛날 것. `아직 모으는 중입니다` → 1~2분 뒤.

### 브리핑의 연속 순매수가 안 나올 때

`holagaram.com/supply_history.json` 맨 앞 `dates` 개수. 3개 미만이면 덜 쌓인 것,
갑자기 1개로 줄었으면 파일이 초기화된 것입니다(8번 탐색기 날짜 항목 참고).

### 서버 상태 확인

```
pm2 list
node --check /root/app/server.js && grep -c "require('./" /root/app/server.js
```

`online` 이고 require 줄이 **21개**면 정상입니다.

### 업종 쏠림지수가 비어 있거나 날짜가 안 늘 때

`holagaram.com/sector_tilt.json` 맨 뒤 `date` 가 어제면 칸 21 → 22 를 안 돌린 것입니다.
업종 이름이 바뀌어 멈추면 `찾지 못한 업종` 줄에 이름이 찍힙니다.

### AI 요약·해석이 "만들지 못했습니다"

```
ANTHROPIC_API_KEY 가 없습니다   Vercel 환경변수 확인 → Redeploy
invalid x-api-key              키가 잘못 들어감
credit balance is too low      충전 필요
401 + 로그인 화면 HTML          middleware 가 POST 를 막음
```

### 타임라인이 안 쌓일 때

```
grep -c ANTHROPIC /root/app/.env     1 이 나와야 정상
curl -s "localhost:3000/timeline/now" | head -c 150
```

장중이 아니면 안 쌓이는 것이 정상. 국내 평일 09:00~15:40, 미국 동부 09:30~16:00.

### 선물 등락률이 이상할 때

3·6·9·12월 셋째 금요일 직후에는 선물 월물이 바뀝니다. `api/macro.js` · `api/macro-spark.js` ·
`us_stocks.js` 가 월물을 콕 집어 받는지 확인합니다 (`grep -c 월물기호`).

### 화면이 통째로 안 뜰 때

`F12` → `Console` 에 빨간 오류. 대개 자바스크립트 문법 오류입니다.

---

## 8. 겪었던 문제와 해결

### 키움 · API

**이중 마이너스** — 음수에 `--1304` 처럼 붙습니다. 부호는 떼고 `pred_pre_sig`(4·5면 하락)로 방향 확정.

**넥스트레이드** — 종목 코드에 `_AL` 을 붙여야 통합 수치.

**`ka20006` 업종일봉** — `base_dt` 가 비면 오류. 오늘 날짜를 넣습니다. 종가는 ×100 으로 옵니다(÷100).
**거래대금 `trde_prica` 는 백만원**입니다(÷100 → 억원). 2026-09-26 엑셀과 100칸 일치 확인.

**`ka10059` 단위** — 금액이 백만원 단위.

**키움 REST 에는 선물·옵션이 없습니다** — 국내주식·미국주식·환전뿐. 선물 투자자별은 네이버 증권
`m.stock.naver.com/api/index/FUT/trend` (하루치, `?bizdate=YYYYMMDD` 로 과거도 줌).
예전 `finance.naver.com/sise/investorDealTrendDay` 는 **410**(폐지)입니다.

### pykrx · KRX

**`KeyError: '지수명'`** ★ — `get_index_ohlcv(…, name_display=False)` 로 피합니다.

**`get_market_cap` 에는 등락률이 없습니다** ★ — `price_cache.csv` 의 마지막 두 날로 계산.

**`get_market_ticker_name` 은 종목마다 접속** ★ — 차단의 가장 큰 원인. `get_market_price_change` 로 시장당 1회.

**거래일 목록을 날짜마다 묻지 마세요** ★ — 지수 일봉 한 번이면 거래일 목록이 통째로 나옵니다.

**KRX 연속 호출 · 로그인 정지** ★ — 간격 1.5~2초, 한 번에 20~40일까지만. 칸 32 는 한 번에 20일,
2일 연속 실패하면 스스로 멈춥니다. **연달아 시도하면 차단이 길어집니다.**

```python
from pykrx import stock
print(stock.get_nearest_business_day_in_a_week("20260914"))   # 풀렸는지 확인
```

**캐시는 최신부터 채웁니다** ★ — `[:40]` 이 아니라 `[-40:]`. 칸 32 도 최근 날부터 받습니다.

### 서버 · 배포

**`sed` 로 서버 파일 수정 금지** — 따옴표가 깨집니다. 5번의 `node -e` 넣기 명령이나 `nano` 를 씁니다.

**명령 속 `!`** ★ — bash 가 예전 명령으로 바꿔 끼워 `SyntaxError: Unexpected number` (2026-09-26).

**`curl` 이 404 를 파일로 저장** — `Unexpected token ':'`.

**GitHub Raw 지연** — 올린 직후 몇 분은 옛 파일이 올 수 있습니다. `grep -c 새기능이름 파일.js` 로 확인.

**요청 안에서 오래 걸리는 일을 하지 마세요** — Vercel 함수는 10~15초(설정하면 60초)에 끊깁니다.
`leader_flow` · `kr_etf_flow` · `fut_flow` 처럼 뒤에서 모아 저장하고 화면에는 저장된 것만 돌려줍니다.

### 데이터

**엑셀 5일 표의 날짜 밀림** — 삼성전자 최근 5거래일 표가 누적 시트보다 20거래일 앞의 숫자를 담고 있었습니다
(2026-09-26 발견). 브리핑은 키움 값을 쓰므로 영향 없음. SK하이닉스 표도 같은 방식이면 확인할 것.

**엑셀 주간 합계의 중복** — 4월5 주(B23:B27)와 5월1 주(B27:B30)가 5/4 를 두 번 더했습니다(+39,109).
브리핑의 주간 막대는 날짜로 묶으므로 이 문제가 없습니다.

**야후 순자산은 늦습니다** — EWY 야후 $27.72B 대 iShares $26.90B. 순자산이 안 바뀐 날을
그대로 비교하면 가짜 유출입이 나와 `순자산 미갱신` 으로 가립니다.

**iShares 날짜 = 한국 거래일** — 미국 날짜 T 의 보유내역 변화는 한국 T 일 매매입니다. 한국 휴장일은 0.

**EWY 의 현물 ≠ 자금** — MSCI Korea 25/50 상한 때문에 SK하이닉스를 환매보다 더 팔았습니다.
20일 현물 −3.5조 대 자금 −2.3조, 차이만큼 현금·선물이 늘었습니다(확인됨).

### 기타

**탐색기의 `수정한 날짜` 를 믿지 마세요** ★★ — 구글 드라이브는 **내용은 최신인데 날짜만 옛날**로 보입니다.
이걸 보고 전일 파일을 지웠다가 `supply_history.json` 이 세 번 초기화됐습니다.
**확인은 파일 안의 `date` 값으로.** 칸 6 이 실행될 때마다 표로 찍어줍니다.

**칸을 붙여넣다 다른 칸이 사라짐** — 2026-09-26 칸 32 를 바꾸다 칸 35·36(신호등)이 없어졌습니다.
칸마다 【이름표】 를 달고 되살렸습니다. **칸을 바꿀 때는 이름표를 보고, 한 칸만** 바꾸세요.

**화면 파일을 여러 번에 나눠 고칠 때** ★ — 먼저 만든 기능 위에 예전 파일을 덮어쓰기 쉽습니다.
올리기 전에 기능이 남아 있는지 한 번 확인하세요. (예: `leader.html` 만 옛 메뉴로 남았던 일)

**노트북이 두 개로 갈라진 것** ★ — 두 컴퓨터에서 동시에 열어 생긴 충돌. 3-2 참고.

**KRX Open API 는 전일까지만** — API 마다 따로 이용신청·승인. 승인 안 됨 `401`, 주소 틀림 `404`.

**서버 시간대** — `timedatectl set-timezone Asia/Seoul`.

---

## 9. 알아두어야 할 한계

**전부 비공식 경로가 섞여 있습니다.** 야후·CNBC·텔레그램·대만거래소·**네이버 증권**·iShares 보유내역은
문서화된 공개 API가 아닙니다. 상대가 구조를 바꾸면 예고 없이 멈춥니다.
멈춰도 그동안 쌓인 날은 남고, 그 항목만 비도록 만들어 두었습니다.

**코스피200 야간은 실제 KRX 선물이 아닙니다.** 해외 거래소(Hyperliquid)의 24시간 계약 가격입니다.

**장중 잠정 수급은 추정치입니다.** 09:30에는 외국인만 나오므로 오전 10시 전에 다른 탭이 비는 건 정상.

**F&G · 시장 신호등은 자체 산출 지표입니다.** 신호등은 5년치로 검증한 조건만 신호로 씁니다.

**RS 는 과거 수익률 기반** — 고점에서 가장 높고, 소형주가 구조적으로 유리합니다.

**신고가·정배열·순매수 순위는 하루 한 번 갱신**, **대형주 수급·미국 ETF 의 한국 매매는 3시간마다**입니다.

**미국 ETF 자금 흐름의 '추정'** 은 야후 순자산이 늦게 바뀌면 틀릴 수 있습니다. '정확' 만 믿으세요.

**시세 재배포 문제.** 국내 지수, 투자자 매매동향, 시총대비 수급, 거래대금 순위는 KRX 시세를 키움 API로
받은 것입니다. 증권사 API 약관은 대체로 본인 투자 목적 이용만 허용하고 제3자 제공을 금지합니다.
유료 구독자에게 보여주는 것은 별도 계약이 필요할 수 있습니다. 키움·코스콤에 문의해 두시기를 권합니다.

---

## 10. 수정할 때

**화면 문구나 숫자** — GitHub 에서 파일을 열고 연필 아이콘 → 수정 → `Commit changes`.

**메뉴 (페이지 추가·순서·이름)** — `menu.js` 의 `메뉴` 목록 한 곳만.

**참고 지표 종목** — `api/macro.js` 의 `INSTRUMENTS`. 미니 그래프는 `api/macro-spark.js` 의 `SYMBOLS`.

**미국 증시 종목** — 서버 `us_stocks.js` 의 `GROUPS`.

**미국 ETF 자금 흐름 목록** — 서버 `us_flow.js` 의 `자금ETF`. State Street 목록은 `스테이트`.

**시총 하한** — 장중 잠정 `supply.js` 의 `MIN_CAP`(1,000억), RS 칸 16 의 `MIN_CAP`(3,000억),
순매수 순위 칸 32 의 `최소시총`(1,000억 — 화면 설명 문구도 따라 바뀜).

**순매수 순위** — 칸 32 의 `한번에최대`(20) · `최소견줄날`(40) · `보일순위`(★ 5위까지) · `줄수`(40).

**대형주 수급** — `leader_flow.js` 의 `TOP_N`(30), `DAYS`(20), `REFRESH_MS`(3시간), 우선주 제외 `SKIP_PREF`.

**히트맵 아래 목록 개수** — `map.html` 의 `TR_LIMIT`(40).

**연속 순매수** — 칸 10 의 `KEEP_DAYS`(12), `TOP_HIST`(50). 연속 판정은 `brief.html` 의 `streaks()`.

**브리핑 투자자 동향** — 모으는 곳 `api/flow-diary.js`(몇 월부터: `시작`), 그리는 곳 `brief.html` 의
`투자자동향()` (4월이후 차트 시작일: `시작 = "20260401"`).

**작업 폴더** — 칸 6 의 `작업폴더`. **로그인 화면 문구** — `middleware.js`.

---

## 11. 비용

```
Vultr           월 $5~6       서버
Vercel          월 $20        Pro (상업적 이용 + 함수 제한 해제)
도메인           연 $11.25     holagaram.com
Claude API      하루 $0.5~1   AI 요약 · 지표 해석 · 타임라인 (쓴 만큼)
```

Claude API 는 미리 충전해 쓰는 방식입니다. 잔액이 0 이 되면 **AI 기능만 멈춥니다.**
자동 충전은 꺼두는 편이 안전합니다. Vercel 지출 한도는 $30 (닿아도 알림만).
