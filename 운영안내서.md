# 홀라가람 증시 대시보드 — 운영 안내서

마지막 정리: 2026년 9월 19일 (업종 쏠림지수 추가) · 주소 https://holagaram.com

코딩을 몰라도 매일 운영하고, 문제가 생겼을 때 원인을 찾을 수 있도록 쓴 문서입니다.
새 대화에서 작업을 이어갈 때 이 문서를 먼저 올리면 구조를 바로 파악할 수 있습니다.

---

## 1. 지금 밀려 있는 일

**시가대비 등락률** — 수급오실레이터의 "등락률 50"은 전일 대비입니다.
시가 대비로도 볼 수 있으면 장중에 살아난 종목이 보입니다.
다만 `ka10032` 응답에 시가가 없어 별도 호출이 필요하고,
넥스트레이드 프리마켓 체결가가 9시 시가와 섞이는지도 확인해야 합니다.
**장중에 HTS의 9시 시가와 대조해본 뒤** 진행하기로 했습니다.

**`fg_extra.json`을 KRX로 완전히 갈아타기** — 지금은 엑셀 우선, 없으면 KRX입니다(6-3 참고).
셋 다 숫자까지 일치하는 것을 확인했으므로 엑셀을 완전히 끊을 수 있지만,
KRX는 전일까지만 주므로 하루 늦어집니다. 당분간 지금 방식을 유지합니다.

**안 하기로 한 것** — RS 상위 탭에 시총 거르기 버튼 추가.
RS 상위 50에 1조 미만이 절반쯤 들어오는데, 이건 고장이 아니라 지표의 성질입니다.
소형주가 더 크게 오르니 수익률 순위에서 위로 갑니다. 버튼을 더 늘리면 복잡해지기만 해서
대형주만 볼 때는 **대형주 수급 페이지**를 쓰기로 했습니다.

---

## 2. 매일 할 일

### 아침 (장 시작 전)

만들 것은 없습니다. 서버가 알아서 돌아갑니다. 보기만 하면 됩니다.

1. `Ctrl+Shift+R` 로 holagaram.com 열기 — 지수·수급에 숫자가 차 있는지
2. **야간 증시** · **미국 증시 정리**(텍스트 복사 버튼) — 밤사이 결과
3. **오늘의 브리핑** — 어제 마감 기준 요약 문장 + 3거래일 연속 순매수

자세한 것은 함께 만든 `운영_체크리스트.html`(인쇄용)을 보세요.

### 장 마감 후 (16시 이후)

**장중에는 돌리지 마세요.** 종가 자리에 현재가가 저장되면 그 날짜는 채워진 것으로 간주돼
다시는 받아오지 않습니다. 잘못된 값이 영구히 남습니다.

1. HTS에서 `피어앤그리드.xlsx` 새로고침
2. `G:\내 드라이브\주식데이터`에서 주피터 실행 (`py -m jupyter lab`)
3. **`KRX_수급비율_Colab__1_.ipynb`** 를 엽니다 (이름 끝에 `(1)` 붙은 쪽. 3-2 참고)
4. **칸을 순서대로** 실행합니다. 건너뛰면 `NameError`가 납니다(2-2 참고)

```
칸 4    KRX 로그인
칸 6    설정 · DATE="auto" 확인 · 현재 폴더 파일 현황이 표로 나옵니다
칸 8    4단계 실행 (시간 걸림)
칸 10   supply_final.json + supply_history.json
칸 12   fg_extra.json  (엑셀 우선, 없으면 KRX 보완)
칸 9    marketcap.json
칸 15   rs.json  (매일 돌려도 10초)
칸 20   sector_tilt.json  (업종 쏠림지수 — 하루치만 이어붙여 20초)
```

5. **여섯 파일**을 GitHub 저장소 **맨 바깥**에 업로드

```
marketcap.json        시가총액 + 종목명·시장 → 시총대비 수급, 히트맵, 프로그램, 대형주 수급
supply_final.json     확정 수급             → 마감 확정 패널, 양매수 탭, 엑셀 사모 목록
supply_history.json   수급 이력(최근 12거래일) → 브리핑의 3거래일 연속 순매수
fg_extra.json         VKOSPI·Put/Call·국채   → F&G 지수
rs.json               RS·신고가·정배열·등락률 → 히트맵 아래 목록, 수급오실레이터 거르기
sector_tilt.json      업종 쏠림지수          → 공포·탐욕 지수 맨 아래 카드
```

`supply_history_bak.json` · `sector_tilt_bak.json` · `price_cache.csv` 는 올리지 않습니다.
주피터 폴더에만 둡니다.

### 2-2. 칸 의존 관계 — 순서를 지켜야 하는 이유

```
칸 8   ← DATE (칸 6)
칸 10  ← INVESTORS · MIN_MARKET_CAP (칸 6) · data · target (칸 8)

나머지 칸은 앞 칸이 필요 없어 언제든 혼자 돌릴 수 있습니다
```

주피터를 껐다 켜면 기억이 사라지므로 **항상 칸 4부터** 시작합니다.
`target is not defined` = 칸 8을 안 돌린 것, `DATE is not defined` = 칸 6을 안 돌린 것입니다.

### 2-3. 칸별 실행 빈도

```
매일      칸 4 · 6 · 8 · 10 · 12 · 9 · 15 · 20
가끔      칸 16 (수급오실레이터 엑셀)
거의 안 씀  칸 11 (index_history — 1년에 한두 번)
            칸 13 (칸 12로 대체, 막아둠)
            칸 14 (엑셀 통계 확인용) · 칸 17 (폴더 쓰기 확인용)
```

**칸 11만 빼고 매일 다 돌려도 됩니다.** 칸 11은 KRX에서 2년치를 두 번 받아
호출이 많고, 매일 갱신해도 화면이 달라지지 않습니다.

### 가끔 (주 1~2회 또는 필요할 때)

```
칸 16  수급오실레이터 엑셀 (차트 80개)
거래대금 / 주간동향 / 데이터 / 곱버스 칸   (다른 노트북에 있음. 3-2 참고)
```

### 매주 일요일 — 비밀번호 변경

1. Vercel → 프로젝트 → `Settings` → `Environments` → `Production`
2. `SITE_PASSWORD` 오른쪽 ⋯ → `Edit` → 새 비밀번호 → 저장
3. `Deployments` → 맨 위 배포 ⋯ → `Redeploy`

재배포까지 해야 적용됩니다. 값은 Secret이라 다시 볼 수 없으니 따로 기록해 두세요.

---

## 3. 전체 구조

```
[데이터 출처]          [Vultr 중계 서버]      [Vercel]          [브라우저]
키움 REST API    →    141.164.40.229   →   api/ 함수들   →   holagaram.com
야후 파이낸스          (고정 IP 필요한
Hyperliquid            것만 경유)
대만거래소·CNBC   ─────────────────────→
```

**Vultr가 필요한 이유** — 키움 API는 등록된 고정 IP에서만 호출을 받습니다.

**Vercel 함수가 필요한 이유** — https 페이지에서 http 서버를 직접 못 부르고(혼합 콘텐츠),
외부 사이트도 브라우저가 직접 부르면 막힙니다(CORS).

### 3-2. 노트북이 두 개로 갈라져 있습니다 ★

구글 드라이브 동기화 충돌로 한 노트북이 둘로 쪼개졌습니다. **합치지 않은 상태입니다.**

| | `KRX_수급비율_Colab__1_.ipynb` | `KRX_수급비율_Colab.ipynb` |
|---|---|---|
| 칸 수 | 19 | 22 |
| `supply_final.json` | ✅ | ✅ |
| `marketcap.json` | ✅ | ❌ |
| `rs.json` | ✅ | ❌ |
| `fg_extra.json` | ✅ | ❌ |
| `index_history.json` | ✅ | ❌ |
| 거래대금 / 주간동향 / `데이터.xlsx` | ❌ | ✅ |

**매일 쓰는 건 `__1_` 쪽입니다.** 거래대금·주간동향·데이터가 필요할 때만 다른 쪽을 엽니다.

어느 쪽인지 헷갈리면 **설정 칸의 `MIN_MARKET_CAP`** 을 보세요.
`0`이면 `__1_`(매일 쓰는 것), `100_000_000_000`이면 다른 쪽입니다.

언젠가 하나로 합치는 게 좋지만, 합치면 그날 저녁 마감 작업이 익숙하지 않아지므로
여유 있을 때 하세요. **두 컴퓨터에서 동시에 같은 노트북을 열지 마세요.**

---

## 4. 페이지 여덟 개

| 페이지 | 주소 | 내용 |
|---|---|---|
| 주간 증시 모니터 | `/` | 국내 지수, 아시아·미국 지표, 거래대금 상위, 장중/확정 수급(+**양매수**), 순매수·순매도 상위 |
| **오늘의 브리핑** | `/brief.html` | 시장 요약 문장 + **3거래일 연속 순매수**(6창구) · 텍스트 복사 |
| 야간 증시 모니터 | `/night.html` | 코스피200 야간, SK하이닉스·삼성전자 추정가, 미국 지표 |
| 미국 증시 정리 | `/us.html` | 129종목 섹터 히트맵, 목록, 텍스트 복사 버튼 |
| 국내 히트맵 | `/map.html` | 업종 히트맵 → 업종 F&G·MACD → 종목 히트맵 → 종목 차트 창 + **신고가·정배열 목록** |
| 공포·탐욕 지수 | `/fg.html` | 코스피·코스닥 F&G, EMA20, MACD + **맨 아래 업종 쏠림지수** |
| 수급오실레이터 | `/flow-oscillator.html` | 종목 캔들 + 수급 MACD, 종목 고르기 탭 5개 + **신고가·정배열 거르기** |
| **대형주 수급** | `/leader.html` | 시총 상위 30종목의 20일 시총대비 수급 + 오실레이터 |

상단 버튼으로 서로 이동합니다. 여덟 페이지 모두 자기를 뺀 일곱 개가 같은 순서로 보입니다.
좁은 화면에서는 제목이 한 줄, 버튼이 그 아래로 내려갑니다.

### 양매수 탭 (주간 증시 모니터)

장중 잠정·마감 확정 양쪽의 투자자 탭 맨 끝에 있습니다.
**외국인 합과 기관합계에 모두 든 종목**이고, 비율은 두 창구를 더한 값입니다.
왼쪽이 양매수, 오른쪽이 양매도이며 합산이 큰(작은) 순으로 줄 세웁니다.

각 목록이 상위 몇 개까지만 오므로 **둘 다 그 안에 들어야** 잡힙니다.
안내 문구에 `각 ○위 이내 겹친 ○종목`으로 표시됩니다.
장중은 이름이 `외국합`, 확정은 `외국인`이라 화면이 둘 다 인식하도록 되어 있습니다.

### 오늘의 브리핑

위쪽은 문장으로 된 시장 요약입니다. 지수·수급·등락종목수·거래대금·업종 상하위 3개·
F&G·신고가와 정배열 종목 수까지. **텍스트 복사** 버튼으로 방송 자료에 바로 붙여넣습니다.

아래는 **3거래일 연속 순매수**입니다. `supply_history.json`을 읽어
각 창구의 시총대비 순매수 상위 50에 사흘 내리 든 종목만 남깁니다.
오른쪽 숫자는 3일 합산 비율이고, 종목을 누르면 히트맵과 같은 차트 창이 뜹니다.
50위 밖으로 하루라도 밀리면 빠지므로, 순매수였어도 목록에 없을 수 있습니다.

### 수급오실레이터 탭 다섯 개

```
상승률 50    거래대금 상위 150 중 상승률 순
프로그램 50  장중 시총대비 프로그램 순매수
잠정 기관    장중 잠정 기관합계 시총대비
사모 20      마감 확정 사모펀드 시총대비
RS 상위      상대강도 상위 (rs.json)
```

모든 목록에 RS 배지가 붙습니다. 90 이상 빨강, 70~89 주황, 그 아래 회색.

**거르기 버튼 네 개**(전체 / 신고가만 / 정배열만 / 신고가+정배열)가 다섯 탭 전부에 있습니다.
종목명 옆 `新`은 60일 신고가, `정`은 정배열입니다.
탭을 옮겨도 거르기 상태가 유지됩니다.

`rs.json`은 시총 3,000억 이상만 담고 있어, 그 아래 종목은 표시가 없고 거를 때 빠집니다.
안내 문구에 `자료없음 ○종목`으로 표시됩니다.

### 히트맵 맨 아래 — 신고가·정배열

탭 세 개(60일 신고가 / 정배열 / 신고가+정배열)와 시장 필터 세 개(전체/코스피/코스닥).
RS 높은 순으로 40개씩, `더 보기`로 펼칩니다. 종목을 누르면 업종에서 누를 때와 같은 차트 창이 뜹니다.
기본 탭은 종목이 많은 **정배열**입니다.

### 공포·탐욕 지수 맨 아래 — 업종 쏠림지수

매일 업종별 등락률에 순위를 매겨 **(상위 4개 합 − 나머지 합) ÷ 전체 업종 수**를 구하고(롱숏),
그 값을 **지수 = 전날 + (1 + 롱숏)** 으로 누적한 뒤 MACD(12·26·9)를 겁니다.

```
오실레이터가 오르는 중   특정 업종으로 자금이 쏠리는 장
오실레이터가 내리는 중   여러 업종이 돌아가며 오르는 순환매 장
```

위 차트는 원본 엑셀과 같은 구성입니다 — 지수(파랑, 왼쪽 축) · 쏠림 오실레이터(빨강, 오른쪽 축) ·
동조화(초록, 오른쪽 축). 아래는 같은 오실레이터를 막대로 그린 것입니다(0 이상 빨강, 미만 파랑).
누적 지수 선은 하루 +1씩 단조롭게만 오르는 값이라 그리지 않습니다(파일에는 그대로 있습니다).
차트 위에 마우스를 올리면 **그날의 주도 업종 4개**가 위쪽 띠에 바뀌어 표시됩니다.

**동조화**는 업종끼리 서로의 30일 상관계수를 전부 구해 평균낸 값입니다.
높으면 업종이 한 몸처럼 움직이는 장(종목을 골라도 지수 따라감, 분산이 안 먹힘),
낮으면 업종끼리 따로 노는 장(업종·종목 선택이 성과를 가름)입니다.
쏠림 오실레이터와는 전체 기간 상관이 −0.11로 사실상 별개 지표입니다.
다만 같은 기간 지수 변동성과는 0.54로 붙어 있고, 이후 20일 수익률과는 0.15밖에 안 됩니다 —
**지금 장을 설명하는 지표지 앞을 맞히는 지표가 아닙니다.**

원본은 엑셀 `특정업종_쏠림지수_국내.xlsx` 의 WI26 26개 업종 상위 5개 방식입니다.
업종 분류만 KRX 기준으로 바꿔 540거래일을 맞대어 보니
**롱숏 상관 0.978 · 오실레이터 상관 0.768 · 오실레이터 부호 일치 90% 이상** 이라 갈아탔습니다.
비교 화면은 `쏠림지수_비교.html`(주피터 폴더, 저장소에는 올리지 않음)에서 다시 볼 수 있습니다.

### 대형주 수급

정렬 다섯 가지 — 수급 강한 순 / 시총 순 / 등락률 순 / 외국인 20일 순 / 기관 20일 순.

**"수급 강한 순"은 누적 총량이 아니라 오실레이터 마지막 값**입니다.
외국인+기관 시총대비 순매수의 20일 누적선에 MACD(5,10,4)를 건 값이라,
많이 산 종목이 아니라 **최근 들어 빨라진 종목**이 위로 옵니다.
누적 총량은 표의 `외국인 20일`·`기관 20일` 열이고, 그 순서로 정렬하는 버튼도 있습니다.

종목명 옆 `차트` 버튼을 누르면 캔들 창이 뜹니다. 행을 누르면 아래 수급 막대만 바뀝니다.
우선주는 제외합니다(삼성전자우 등, 종목명이 '우'·'우B'로 끝나면 뺍니다).

---

## 5. 서버가 하는 일 (Vultr `/root/app/`)

| 파일 | 하는 일 | API |
|---|---|---|
| `server.js` | 중계 서버 본체 (3000번 포트) + 수급오실레이터 | ka10001 외 |
| `intraday.js` | 국내 지수·투자자 매매동향·종목 시세 | ka20005, ka20001, ka10051, ka10001 |
| `supply.js` | 장중 잠정 시총대비 수급 | ka10065 |
| `trade_rank.js` | 거래대금 상위 150 · 상승률 50 | ka10032 |
| `program_flow.js` | 시총대비 프로그램 순매수 | ka90003 |
| `sector_map.js` | 업종 히트맵 · 업종 종목 · 업종 F&G/MACD | ka20003, ka20002, ka20006 |
| `fear_greed.js` | 코스피·코스닥 F&G | ka20006 + fg_extra.json |
| `flow_history.js` | 일별 시장 수급 이력 (엑셀용) | ka10051 |
| `stock_flow.js` | 종목별 일별 수급 (엑셀용) | ka10059 |
| `us_stocks.js` | 미국 129종목 등락률 | (야후) |
| `krx_investor_rank.js` | 순매수 상위 10 — 평일 18:30 크론 | ka90009 |
| **`leader_flow.js`** | 대형주 수급 — 3시간마다 혼자 수집 | ka10059 |

`server.js`에 `require('./파일')(app);` 줄이 **12개** 있어야 정상입니다.

**중요한 규칙** — 종목·업종 코드 뒤에 `_AL`을 붙여야 넥스트레이드까지 합친 통합 수치가 나옵니다.
HTS 화면과 값이 다르면 이것부터 의심합니다.

**`leader_flow.js`는 요청을 기다리게 하지 않습니다.** 서버가 뜨고 30초 뒤, 그다음 3시간마다
혼자 60종목을 돌아 `/root/app/leader_flow.json`에 저장하고, 화면 요청에는 저장된 파일을 즉시 돌려줍니다.
요청 안에서 모으면 20초 넘게 걸려 Vercel 함수가 끊깁니다.

**서버 파일 올리는 법** — GitHub 맨 바깥에 올린 뒤 Vultr에서 받아갑니다.
비밀번호에 막히지 않도록 반드시 Raw 주소를 씁니다.

```
cd /root/app && curl -s -o 파일.js https://raw.githubusercontent.com/ramgalee/night-dashboard/main/파일.js && node --check 파일.js && pm2 restart all
```

`node --check`에서 `Unexpected token ':'` 가 나오면 GitHub에 그 파일이 없어서
`404: Not Found`라는 글자가 저장된 것입니다. 받은 파일을 지우고 업로드부터 다시 하세요.

새 기능이면 `server.js`에 `require('./파일')(app);` 줄도 추가해야 합니다.
`sed`는 따옴표를 깨뜨린 적이 있으니 `nano`로 직접 고치세요.
`app.listen` 줄보다 **위에** 넣어야 합니다.

---

## 6. 파일이 어디에 있나

### GitHub 저장소 (= Vercel 배포)

```
index.html  brief.html  night.html  us.html  map.html  fg.html
flow-oscillator.html  leader.html
middleware.js           비밀번호 보호
marketcap.json          시가총액 + info(종목명·시장)  (장 마감 후 교체)
supply_final.json       확정 수급                     (장 마감 후 교체)
supply_history.json     수급 이력 최근 12거래일        (장 마감 후 교체)
fg_extra.json           VKOSPI·Put/Call·국채          (장 마감 후 교체)
rs.json                 RS·신고가·정배열·등락률        (매일 또는 주 1~2회)
index_history.json      지수 이력 씨앗 (지우지 말 것)
운영안내서.md            이 문서
package.json
api/                    Vercel 함수들
leader_flow.js          서버로 옮기는 통로
sector_map.js           서버로 옮기는 통로
```

`api/` — `market.js`, `macro.js`, `macro-spark.js`, `twse.js`, `kr200.js`, `us-stocks.js`,
`telegram-news.js`, `index-intraday.js`, `investor-rank.js`, `supply-ratio.js`,
`trade-rank.js`, `program-flow.js`, `flow-oscillator.js`, `fear-greed.js`,
`sector-map.js`, `sector-stocks.js`, `sector-indicator.js`, **`leader-flow.js`**

Vercel 함수는 전부 `export default async function handler(req, res)` 형식입니다.
`module.exports` 방식으로 쓰지 마세요. Pro 플랜이라 함수 개수 제한은 없습니다.

### 주피터 (`G:\내 드라이브\주식데이터`)

| 노트북 | 하는 일 |
|---|---|
| `KRX_수급비율_Colab__1_` | **매일 쓰는 것.** 확정 수급, 시가총액, F&G 보조, RS, 수급오실레이터 엑셀 |
| `KRX_수급비율_Colab` | 거래대금, 주간동향, 데이터, 곱버스 |
| `program_realtime` | 프로그램 수급 CSV → 엑셀 파워쿼리 (CSV는 `C:\Users\user\Documents\`) |
| `장중잠정_시총대비수급` | 이제 서버가 대신하므로 안 써도 됨 |

### 6-2. 쌓아가는 파일 — 절대 지우지 마세요 ★

```
price_cache.csv          110거래일 종가 — 지우면 하루 40일씩 사흘 걸려 복구
supply_history.json      수급 이력 — 지우면 3거래일 다시 모아야 함
supply_history_bak.json  그 백업 (칸 10이 매번 함께 저장)
fg_extra.json            과거치 누적 (칸 12가 기존 것을 읽어 이어 붙임)
sector_tilt.json         업종 쏠림지수 누적 (칸 20이 기존 것을 읽어 이어 붙임)
sector_tilt_bak.json     그 백업 (칸 20이 매번 함께 저장)
index_history.json       지수 씨앗
```

**매일 새로 만들어도 되는 것**은 `marketcap.json` · `supply_final.json` · `rs.json` 셋뿐입니다.

`supply_history.json`이 사라져도 `supply_history_bak.json`에서 자동으로 되살아납니다.
칸 10이 실행될 때마다 `[수급이력] 본 파일 ○일 · 백업 ○일` 이 찍히므로 그 줄만 보면 됩니다.

### 6-3. fg_extra.json 의 두 갈래 (칸 12)

```
1) 피어앤그리드.xlsx 를 읽어 채웁니다          — 지금까지와 동일, 당일 값
2) 엑셀이 없거나 날짜가 모자라면 KRX 로 채웁니다 — 전일까지만
```

엑셀이 이미 채운 날짜는 건드리지 않습니다. 두 출처의 값이 같다는 것을 확인했으므로
섞여도 문제없습니다. KRX 인증키는 칸 12 안의 `KRX_KEY` 에 넣습니다.

| 항목 | KRX API | 찾는 값 |
|---|---|---|
| `vkospi` | `/idx/drvprod_dd_trd` | `IDX_NM`이 `코스피 200 변동성지수`인 줄의 `CLSPRC_IDX` |
| `bondDiff` | 같은 API | `10년국채선물지수` − `5년 국채선물 추종 지수` |
| `putCall` | `/drv/opt_bydd_trd` | `PROD_NM`이 `코스피200 옵션`인 전 종목 `ACC_TRDVOL` 합계(정규+야간)를<br>콜·풋 각각 5일 이동평균 낸 뒤 풋÷콜 |

엑셀의 `최근월물 CALL/PUT ATM` 열은 이름과 달리 **ATM 옵션 가격이 아니라
콜·풋 전체 거래량의 5일 이동평균**입니다. 값에 5를 곱하면 정수가 되는 것으로 확인했습니다.

### sector_tilt.json 구조

```
generatedAt, topN: 4, corrWindow: 30
sectors: { KOSPI: [업종명…], KOSDAQ: [업종명…] }
data:    { KOSPI: [{date, tilt, index, close, corr,
                    top:[주도업종 4개], macd, signal, osc}, …],
           KOSDAQ: [같은 구조] }
```

`tilt`이 롱숏, `index`가 누적 지수, `close`는 그날 코스피(코스닥) 종가,
`corr`가 동조화입니다. `close`가 없는 날은 화면이 `index_history.json`에서 대신 찾아 채웁니다.

칸 20은 쏠림 값만 마지막 날짜 다음부터 이어 붙이고,
**MACD·지수 종가·동조화는 매번 전체를 다시 계산**합니다.
그래서 업종 지수를 전 구간(2024-07-02~) 받아오지만 호출 수는 업종 수와 같습니다.

### rs.json 구조

```
date, generatedAt, count, minCap, bars, cacheLast, trendValid
data: { 종목코드: { name, market, close, chg, rs, m3, m6, m9, m12, high60, aligned } }
```

`chg`는 캐시의 마지막 두 날 종가로 계산한 등락률입니다. KRX를 더 부르지 않습니다.
`trendValid`가 `false`면 종가가 부족해 신고가·정배열을 계산하지 않은 것입니다.

---

## 7. 문제가 생겼을 때

### 먼저 `Ctrl + Shift + R`

"분명 올렸는데 화면이 그대로"의 대부분은 브라우저 캐시입니다.
서버까지 파고들기 전에 이것부터 해보세요.

### 화면 일부가 "불러오지 못했습니다"

브라우저에서 해당 주소를 직접 열어 어느 단계인지 봅니다.

```
https://holagaram.com/api/index-intraday
```

- 데이터가 나오면 → 브라우저 문제. `Ctrl+Shift+R`
- `{"error":"..."}` → 그 메시지가 원인
- `404 NOT_FOUND` → 파일이 `api` 폴더 밖에 있거나 이름이 틀림

### 어디까지 정상인지 한 번에 가르기

서버 → Vercel → 브라우저 순서로 같은 것을 봅니다.

```
curl -s localhost:3000/fear-greed?days=3 | tail -c 300      ← 서버
https://holagaram.com/api/fear-greed?days=3                 ← Vercel
```

서버는 맞는데 Vercel이 다르면 Vercel 캐시, 둘 다 맞는데 화면이 다르면 브라우저 캐시입니다.

### 수급오실레이터 차트가 안 나올 때

대개 키움 토큰 문제이고 재시작으로 풀립니다.

```
pm2 restart all
```

### 여러 기능이 한꺼번에 500 오류일 때

키움을 안 쓰는 기능(`us-stocks`, `fear-greed`)이 정상이면 키움 문제입니다.

```
for p in index-intraday fear-greed us-stocks trade-rank; do echo -n "$p: "; curl -s -o /dev/null -w "%{http_code}\n" localhost:3000/$p; done
```

### 대형주 수급이 비어 있을 때

```
curl -s localhost:3000/leader-flow | head -c 200
pm2 logs --lines 40 --nostream | grep leader_flow
```

- `info(종목명·시장)가 없습니다` → `marketcap.json`이 옛날 것. 새로 만들어 올리세요
- `아직 모으는 중입니다` → 1~2분 기다렸다 다시
- 로그의 `완료 ○초 · 코스피 30 · 코스닥 30 · 실패 0` 이 정상입니다

### 브리핑의 연속 순매수가 안 나올 때

```
https://holagaram.com/supply_history.json
```

맨 앞 `dates` 에 날짜가 몇 개인지 봅니다. 3개 미만이면 아직 덜 쌓인 것이고,
갑자기 1개로 줄었으면 파일이 초기화된 것입니다(8번 탐색기 날짜 항목 참고).

### 서버 상태 확인

```
pm2 list
node --check /root/app/server.js && grep -c "require('./" /root/app/server.js
```

`online`이고 require 줄이 12개면 정상입니다.

### 업종 쏠림지수가 비어 있거나 날짜가 안 늘 때

```
https://holagaram.com/sector_tilt.json
```

맨 뒤 `date` 가 어제 날짜면 칸 20을 안 돌린 것입니다.
`불러오지 못했습니다` 가 뜨면 저장소 **맨 바깥**에 파일이 없는 것입니다(`api` 폴더 아님).
업종 이름이 바뀌어 칸 20이 멈추면 `찾지 못한 업종` 줄에 이름이 찍힙니다 —
그 이름을 `sectors` 목록에서 새 이름으로 고치면 됩니다.

### 화면이 통째로 안 뜰 때

`F12` → `Console`에 빨간 오류가 있습니다. 대개 자바스크립트 문법 오류입니다.

---

## 8. 겪었던 문제와 해결

### 키움 · API

**응답의 이중 마이너스** — 음수에 `--1304`처럼 마이너스가 두 개 붙습니다.
부호는 따로 떼고 숫자만 읽은 뒤 `pred_pre_sig`(4·5면 하락)로 방향을 확정합니다.

**넥스트레이드** — 종목 코드에 `_AL`을 붙여야 통합 수치가 나옵니다.

**`ka20006` 업종일봉** — `base_dt`가 비어 있으면 오류가 납니다. 오늘 날짜를 넣어야 합니다.

**`ka20003` 전업종지수** — `mrkt_tp`와 `inds_cd`를 함께 줘야 합니다.

**`ka10059` 단위** — 금액이 백만원 단위로 옵니다. 원으로 바꾸려면 `1e6`을 곱합니다.

### pykrx · KRX

**`KeyError: '지수명'`** ★ — `get_index_ohlcv`가 지수 이름을 붙이려다 터집니다.
**일봉은 이미 받아온 뒤**에 나는 오류라 `name_display=False`를 주면 그냥 지나갑니다.

```python
stock.get_index_ohlcv(시작, 끝, "1001", name_display=False)
```

**`get_market_cap`에는 등락률이 없습니다** ★ — 열은 종가·시가총액·거래량·거래대금·상장주식수입니다.
등락률이 필요하면 `price_cache.csv`의 마지막 두 날로 계산하세요. 호출이 늘지 않습니다.

**`get_market_ticker_name`은 종목마다 접속합니다** ★ — 2,765종목이면 2,765번입니다.
차단의 가장 큰 원인이었습니다. `get_market_price_change`를 쓰면 시장당 1회로 끝납니다.

**거래일 목록을 날짜마다 묻지 마세요** ★ — `get_nearest_business_day_in_a_week`를
달력 날짜마다 부르면 154번쯤 됩니다. 지수 일봉 한 번이면 거래일 목록이 통째로 나옵니다.

**KRX 연속 호출** — 간격을 2초 이상 두고, 한 번에 40일까지만 받습니다.
차단이 풀렸는지는 이 한 줄로 확인하고, **연달아 시도하면 차단이 길어집니다.**

```python
from pykrx import stock
print(stock.get_nearest_business_day_in_a_week("20260914"))
```

**기준일이 오늘이 아닐 때** — 장 마감 직후에는 KRX 게시까지 시간이 걸려
지난 거래일이 기준이 됩니다. 정상이며 다음 날이면 맞춰집니다.

**캐시를 오래된 날짜부터 채우면 안 됩니다** ★ — 받을 날짜를 `[:40]`으로 자르면
가장 오래된 날부터 채워져, 계산이 쓰는 "마지막 줄"이 몇 달 전 종가가 됩니다.
`[-40:]`로 최신부터 채워야 합니다.

### 서버 · 배포

**`sed`로 서버 파일 수정** — 따옴표가 깨져 `require` 줄이 망가진 적이 있습니다.
`nano`를 쓰고, 수정 후 `node --check`와 `grep -c "require('./"`로 확인합니다.

**`curl`이 404를 파일로 저장** — GitHub에 없는 파일을 받으면 "404: Not Found"라는
글자가 저장되고, `node --check`에서 `Unexpected token ':'` 가 납니다.

**Vercel 함수 형식** — 기존 함수들은 전부 `export default async function handler`입니다.
`module.exports`로 쓰면 어긋납니다.

**요청 안에서 오래 걸리는 일을 하지 마세요** — Vercel 함수는 10~15초에 끊깁니다.
`leader_flow`처럼 20초 넘게 걸리는 수집은 뒤에서 돌리고 저장된 파일만 돌려줍니다.

### 기타

**탐색기의 `수정한 날짜`를 믿지 마세요** ★★ — 가장 많이 당한 것입니다.
구글 드라이브는 클라우드의 시각을 로컬 파일에 씌우기 때문에, **내용은 최신인데
날짜만 옛날로 보입니다.** 세 파일의 수정시각이 초 단위까지 똑같으면 그 경우입니다.

이걸 보고 "저장이 안 됐다"고 판단해 전일 파일을 지우고 다시 만들다가,
`supply_history.json`이 세 번이나 초기화됐습니다.

**확인은 파일 안의 `date` 값으로 합니다.** 칸 6이 실행될 때마다 표로 찍어줍니다.

```
[현재 폴더 파일]
  supply_history.json     24,830B  09/18 16:19  1일 20260918
                                   ↑ 못 믿음     ↑ 이걸 봅니다
```

참고로 `G:` 쓰기 자체는 정상입니다(2026-09-19 진단). 덮어쓰기·되돌아감 모두 이상 없었습니다.

**구글 드라이브 `G:` 쓰기 실패** — 드물게 진짜로 실패할 때가 있습니다.
구글 드라이브를 재시작하거나 저장 경로를 `C:`로 우회합니다.
칸 6의 `작업폴더` 변수에 경로를 넣으면 한 번에 바뀝니다.

**노트북이 두 개로 갈라진 것** ★ — 두 컴퓨터에서 동시에 열어 생긴 충돌입니다. 3-2 참고.

**쌓아가는 파일을 지우고 다시 만들지 마세요** ★ — 6-2 참고.
덮어쓰기가 안 되는 것처럼 보여도 실제로는 되고 있습니다.

**`get_market_cap`에 `등락률` 열이 없습니다** — `rs.json`의 `chg`는
`price_cache.csv`의 마지막 두 날 종가로 계산합니다. KRX 호출이 늘지 않습니다.

**KRX Open API는 전일까지만** — `openapi.krx.co.kr`. 인증키를 받은 뒤
**API마다 따로 이용신청·승인**을 받아야 합니다. 승인 안 된 API는 `401`,
주소가 틀리면 `404`로 구분됩니다. 지수 서비스 안에서도 API별로 따로 신청해야 했습니다.

**서버 시간대** — `timedatectl set-timezone Asia/Seoul`로 맞춰뒀습니다.

**화면 파일을 여러 번에 나눠 고칠 때** ★ — 먼저 만든 기능 위에 예전 파일을 덮어쓰기 쉽습니다.
실제로 `flow-oscillator.html`과 `map.html`에서 두 번 일어났습니다.
새 파일을 받으면 **올리기 전에 기능이 남아 있는지 한 번 확인**하세요.

---

## 9. 알아두어야 할 한계

**전부 비공식 경로입니다.** 야후·CNBC·텔레그램·대만거래소 모두 문서화된 공개 API가 아닙니다.
상대가 구조를 바꾸면 예고 없이 멈춥니다.

**코스피200 야간은 실제 KRX 선물이 아닙니다.** 해외 거래소(Hyperliquid)의 24시간 계약 가격입니다.

**장중 잠정 수급은 추정치입니다.** 키움이 하루 몇 차례 집계하며 시각은 매일 다릅니다.
09:30에는 외국인만 나오므로 오전 10시 전에 다른 탭이 비어 있는 건 정상입니다.

**F&G는 자체 산출 지표입니다.** 코스피·코스닥은 다섯 항목(모멘텀·RSI·VKOSPI·Put/Call·국채),
업종은 두 항목(모멘텀·RSI)으로 계산합니다. 항목이 달라 값의 성격도 다릅니다.

**RS는 과거 수익률 기반입니다.** 고점에서 가장 높게 나오고, 소형주가 구조적으로 유리합니다.
상위 50에 1조 미만이 절반쯤 들어오는 것은 정상입니다. 수급·차트와 함께 보세요.

**신고가·정배열은 하루 한 번 갱신입니다.** 60일 신고가는 60**거래일**(약 3개월) 종가 기준,
정배열은 5일선 > 20일선 > 60일선. 당일 장중 움직임은 반영되지 않습니다.

**대형주 수급은 3시간마다 갱신입니다.** 장중 실시간이 아닙니다.

**시세 재배포 문제.** 국내 지수, 투자자 매매동향, 시총대비 수급, 거래대금 순위는 모두
KRX 시세를 키움 API로 받은 것입니다. 증권사 API 약관은 대체로 본인의 투자 목적으로만
이용을 허용하고 제3자 제공을 금지합니다. 유료 구독자에게 보여주는 것은 별도 계약이
필요할 수 있습니다. 키움 고객센터나 코스콤에 문의해두시기를 권합니다.

---

## 10. 수정할 때

**화면 문구나 숫자** — GitHub에서 파일을 열고 연필 아이콘 → 수정 → `Commit changes`.

**참고 지표 종목** — `api/macro.js`의 `INSTRUMENTS`. 미니 그래프는 `api/macro-spark.js`의 `SYMBOLS`에도 추가.

**미국 증시 종목** — Vultr `us_stocks.js`의 `GROUPS`.

**시총 하한** — 장중 잠정은 `supply.js`의 `MIN_CAP`(1,000억), RS는 `rs_만들기` 칸의 `MIN_CAP`(3,000억).

**대형주 수급 종목 수·기간** — `leader_flow.js`의 `TOP_N`(30), `DAYS`(20), `REFRESH_MS`(3시간).
우선주 제외는 `SKIP_PREF`.

**히트맵 아래 목록 개수** — `map.html`의 `TR_LIMIT`(40).

**연속 순매수 일수·보관 기간** — 칸 10의 `KEEP_DAYS`(12), `TOP_HIST`(50).
연속 판정은 `brief.html`의 `streaks()` 안 `slice(-3)`.

**작업 폴더** — 칸 6의 `작업폴더` 변수. `None` 이면 지금 폴더를 그대로 씁니다.

**엑셀 종목 수** — 수급오실레이터 엑셀 칸의 `TOP_UP`(50), `TOP_SAMO`(30).

**로그인 화면 문구** — `middleware.js` 안의 제목과 안내.

---

## 11. 비용

```
Vultr    월 $5~6      서버
Vercel   월 $20       Pro (상업적 이용 + 함수 제한 해제)
도메인    연 $11.25    holagaram.com
```

Vercel 지출 한도는 $30입니다. 한도에 닿아도 사이트는 멈추지 않고 알림만 옵니다.
