# 홀라가람 증시 대시보드 — 운영 안내서

마지막 정리: 2026년 9월 9일 · 주소 https://holagaram.com

코딩을 몰라도 매일 운영하고, 문제가 생겼을 때 원인을 찾을 수 있도록 쓴 문서입니다.

---

## 1. 매일 할 일

### 아침 (장 시작 전)

특별히 없습니다. 서버가 알아서 돌아갑니다.

### 장 마감 후 (16시 이후)

주피터에서 파일 두 개를 만들어 GitHub에 올립니다. 5분이면 끝납니다.

1. `G:\내 드라이브\주식데이터` 폴더에서 주피터를 엽니다
2. `KRX_수급비율_Colab` 노트북에서 **로그인 칸** 실행 (KRX 아이디·비밀번호)
3. **`marketcap.json` 만드는 칸** 실행
4. **4단계 칸** 실행 후 **`supply_final.json` 만드는 칸** 실행
5. 두 파일을 GitHub 저장소 **맨 바깥**에 업로드
   (`Add file` → `Upload files` → 끌어다 놓기 → `Commit changes`)

**왜 필요한가.** KRX가 로그인을 요구해서 서버에서 자동으로 못 받습니다. 비밀번호를 서버에 두지 않기로 해서 이 방식을 씁니다.

**안 올리면.** 대시보드는 계속 돌지만 어제 파일을 씁니다. 그날 신규 상장 종목이 빠지고 확정수급 패널이 옛 날짜로 남습니다.

**확인 방법.** 주간 페이지의 "시총대비 수급" 패널 오른쪽 위 날짜가 어제나 오늘이면 정상입니다.

### 매주 일요일

비밀번호를 바꿉니다.

1. Vercel → 프로젝트 → `Settings` → `Environments` → `Production`
2. `Environment Variables`에서 `SITE_PASSWORD` 오른쪽 점 세 개 → `Edit` → 새 비밀번호 → 저장
3. `Deployments` 탭 → 맨 위 배포의 점 세 개 → `Redeploy`

**재배포까지 해야 적용됩니다.** 기존 접속자는 자동 로그아웃되어 새 비밀번호를 넣어야 합니다.

---

## 2. 전체 구조

```
[데이터 출처]          [Vultr 중계 서버]      [Vercel]          [브라우저]
키움 REST API    →    141.164.40.229   →   api/ 함수들   →   holagaram.com
야후 파이낸스          (고정 IP 필요한
Hyperliquid            것만 경유)
대만거래소·CNBC   ─────────────────────→
```

**Vultr가 필요한 이유.** 키움 API는 등록된 고정 IP에서만 호출을 받습니다. Vercel은 IP가 매번 바뀌어 직접 못 부릅니다.

**Vercel 함수가 필요한 이유.** 대시보드는 https인데 중계 서버는 http라 브라우저가 직접 부르면 차단됩니다. 야후 같은 외부 사이트도 마찬가지입니다(CORS).

---

## 3. 페이지 네 개

| 페이지 | 주소 | 내용 |
|---|---|---|
| 주간 증시 모니터 | `/` | 국내 지수, 아시아·미국 지표, 거래대금 상위, 장중/확정 수급, 순매수·순매도 상위 |
| 야간 증시 모니터 | `/night.html` | 코스피200 야간, SK하이닉스·삼성전자 추정가, 미국 지표 |
| 미국 증시 정리 | `/us.html` | 129종목 섹터 히트맵, 전체 목록, 텍스트 복사 버튼 |
| 수급오실레이터 | `/flow-oscillator.html` | 종목별 캔들 + 수급 MACD, 종목 고르기 목록 |

서로 상단 버튼으로 이동합니다.

---

## 4. 서버가 하는 일 (Vultr `/root/app/`)

| 파일 | 하는 일 | 키움 API |
|---|---|---|
| `server.js` | 중계 서버 본체 (3000번 포트) | — |
| `intraday.js` | 국내 지수·투자자 매매동향·종목 시세 | ka20005, ka20001, ka10051, ka10001 |
| `supply.js` | 장중 잠정 시총대비 수급 | ka10065 |
| `trade_rank.js` | 거래대금 상위 150 · 상승률 50 | ka10032 |
| `program_flow.js` | 시총대비 프로그램 순매수 | ka90003 |
| `flow_history.js` | 일별 시장 수급 이력 (엑셀용) | ka10051 |
| `stock_flow.js` | 종목별 일별 수급 (엑셀용) | ka10059 |
| `us_stocks.js` | 미국 129종목 등락률 | (야후) |
| `krx_investor_rank.js` | 순매수 상위 10 — 평일 18:30 크론 | ka90009 |

**중요한 규칙.** 종목·업종 코드 뒤에 `_AL`을 붙여야 넥스트레이드까지 합친 통합 수치가 나옵니다. 안 붙이면 KRX 정규장만 잡혀 HTS 화면과 값이 달라집니다.

**서버 파일 올리는 법.** GitHub 저장소 맨 바깥에 올린 뒤, Vultr에서 아래처럼 받아갑니다. 비밀번호에 막히지 않도록 Raw 주소를 씁니다.

```
cd /root/app && curl -s -o 파일이름.js https://raw.githubusercontent.com/ramgalee/night-dashboard/main/파일이름.js && node --check 파일이름.js && pm2 restart all
```

받아간 뒤 GitHub의 파일은 지워도 됩니다.

---

## 5. 파일이 어디에 있나

### GitHub 저장소 (= Vercel 배포)

```
index.html              주간 증시 모니터
night.html              야간 증시 모니터
us.html                 미국 증시 정리
flow-oscillator.html    수급오실레이터
middleware.js           비밀번호 보호
marketcap.json          시가총액 (장 마감 후 교체)
supply_final.json       확정 수급 (장 마감 후 교체)
package.json
api/                    Vercel 함수 13개
```

`api/` 안에는 `market.js`, `macro.js`, `macro-spark.js`, `twse.js`, `kr200.js`, `us-stocks.js`, `telegram-news.js`와 중계 서버를 부르는 `index-intraday.js`, `investor-rank.js`, `supply-ratio.js`, `trade-rank.js`, `program-flow.js`, `flow-oscillator.js`가 있습니다.

Pro 플랜이라 함수 개수 제한은 없습니다.

### 주피터 (`G:\내 드라이브\주식데이터`)

구글 드라이브 동기화 폴더입니다. 집·사무실 어디서든 같은 파일을 씁니다.

**두 컴퓨터에서 동시에 같은 노트북을 열지 마세요.** 충돌본이 생겨 어느 게 최신인지 헷갈립니다.

| 노트북 | 하는 일 |
|---|---|
| `KRX_수급비율_Colab` | 확정 수급, 시가총액, 거래대금, 주간 매매동향, 데이터, 곱버스 누적 |
| `program_realtime` | 프로그램 수급 실시간 (이제 서버가 대신하므로 안 써도 됨) |
| `장중잠정_시총대비수급` | 장중 잠정 수급 (이제 서버가 대신하므로 안 써도 됨) |

---

## 6. 문제가 생겼을 때

### 화면 일부가 "불러오지 못했습니다"

어느 단계에서 끊겼는지부터 봅니다. 브라우저에서 해당 주소를 직접 엽니다.

```
https://holagaram.com/api/index-intraday
```

- 데이터가 나오면 → 브라우저 문제. `Ctrl+Shift+R`
- `{"error":"..."}` → 그 메시지가 원인
- `404 NOT_FOUND` → 파일이 `api` 폴더 밖에 있거나 이름이 틀림

### 국내 지수·수급이 안 나올 때

Vultr에서 서버가 뭐라고 하는지 봅니다.

```
curl -s localhost:3000/index-intraday | head -c 300
```

`8005: Token`이 보이면 키움 토큰 문제인데 자동으로 다시 받도록 되어 있어 보통 저절로 풀립니다. 안 풀리면 `pm2 restart all`.

### 서버 상태 확인

```
pm2 list
```

`online`이면 정상입니다. Vultr 콘솔 창을 닫아도 서버는 돌아갑니다. 다만 Vultr 사이트에서 `Stop`이나 `Destroy`는 누르면 안 됩니다.

### 화면이 통째로 안 뜰 때

`F12` → `Console` 탭에 빨간 오류가 있습니다. 대개 자바스크립트 문법 오류이고, 같은 코드가 두 번 들어갔거나 화면 요소가 없는데 부른 경우입니다.

### 야후 지표가 멈췄을 때

호출이 막혔을 수 있습니다. 기다렸다가 다시 보고, 계속 안 되면 `index.html`의 `setInterval(fetchMacro, ...)` 값을 늘립니다.

---

## 7. 겪었던 문제와 해결

앞으로 비슷한 일이 생기면 참고하시라고 적어둡니다.

**키움 응답의 이중 마이너스.** 음수에 `--1304`처럼 마이너스가 두 개 붙어 오는 경우가 있습니다. 부호는 따로 떼고 숫자만 읽은 뒤 `pred_pre_sig`(4·5면 하락)로 방향을 확정합니다.

**야후 전일 종가.** 요약으로 주는 `previousClose`와 `chartPreviousClose`가 장중에 엉뚱한 값으로 바뀝니다. 일봉 목록에서 직접 찾되, 개장 직후에는 오늘 봉이 없을 수 있어 마지막 봉 종가와 현재가를 비교해 판단합니다.

**대만 가권지수.** 야후는 20분 지연이라 대만거래소에 직접 연결했습니다. 5분 데이터는 개장 직후 전날 것이 오므로 날짜를 확인해 다르면 그래프를 그리지 않습니다.

**넥스트레이드.** 종목 코드에 `_AL`을 붙여야 통합 수치가 나옵니다. HTS 화면과 값이 다르면 이것부터 의심합니다.

**서버 시간대.** UTC로 되어 있으면 크론이 9시간 어긋납니다. `timedatectl set-timezone Asia/Seoul`로 맞춰뒀습니다.

---

## 8. 알아두어야 할 한계

**전부 비공식 경로입니다.** 야후·CNBC·텔레그램·대만거래소 모두 문서화된 공개 API가 아니라 각 사이트가 자기 화면을 그릴 때 쓰는 주소를 빌려 씁니다. 상대가 구조를 바꾸면 예고 없이 멈춥니다.

**코스피200 야간은 실제 KRX 선물이 아닙니다.** 해외 거래소(Hyperliquid)에서 형성된 24시간 계약 가격입니다. 거래량이 얕아 값이 튈 수 있습니다. 화면의 주의 문구를 지우지 마세요.

**장중 잠정 수급은 추정치입니다.** 키움이 하루 몇 차례(대략 09:30, 10:00, 11:30, 13:20, 14:30) 집계하며 시각은 매일 다릅니다. 09:30에는 외국인만 나오므로 오전 10시 전에 다른 탭이 비어 있는 건 정상입니다.

**시세 재배포 문제.** 국내 지수, 투자자 매매동향, 시총대비 수급, 거래대금 순위는 모두 KRX 시세를 키움 API로 받은 것입니다. 증권사 API 약관은 대체로 본인의 투자 목적으로만 이용을 허용하고 제3자 제공을 금지합니다. 유료 구독자에게 보여주는 것은 별도 계약이 필요할 수 있습니다. 키움 고객센터나 코스콤에 문의해두시기를 권합니다.

---

## 9. 수정할 때

**화면 문구나 숫자** — GitHub에서 파일을 열고 연필 아이콘 → 수정 → `Commit changes`. 1~2분 뒤 자동 배포.

**참고 지표 종목 추가·삭제** — `api/macro.js`의 `INSTRUMENTS` 목록. 미니 그래프도 나오게 하려면 `api/macro-spark.js`의 `SYMBOLS`에도 같은 티커 추가.

**미국 증시 종목** — Vultr `us_stocks.js`의 `GROUPS` 목록.

**시총 하한** — Vultr `supply.js`의 `MIN_CAP`. 지금은 1,000억.

**로그인 화면 문구** — `middleware.js` 안의 제목과 안내 문구.

**서버 파일 수정** — Vultr 콘솔에서. 모바일로는 실수하기 쉬우니 PC에서 하세요.

---

## 10. 비용

```
Vultr    월 $5~6      서버
Vercel   월 $20       Pro (상업적 이용 + 함수 제한 해제)
도메인    연 $11.25    holagaram.com
```

Vercel 지출 한도는 $30으로 설정해두었습니다. 한도에 닿아도 사이트는 멈추지 않고 알림만 옵니다.
