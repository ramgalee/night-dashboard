# 야간증시 모니터 — 운영 안내서

마지막 정리: 2026년 9월

이 문서는 코딩을 몰라도 대시보드를 운영하고, 문제가 생겼을 때 원인을 찾을 수 있도록 쓴 안내서입니다.

---

## 1. 전체 구조

데이터가 화면에 나오기까지 세 곳을 거칩니다.

```
[데이터 출처]        [Vultr 중계 서버]        [Vercel]           [브라우저]
키움 REST API   →    141.164.40.229    →   api/ 폴더의    →   index.html
야후 파이낸스         (고정 IP 필요한                함수들
Hyperliquid           것만 여기 경유)
대만거래소 등     ────────────────────────→
```

**Vultr 중계 서버가 필요한 이유.** 키움 API는 미리 등록해둔 고정 IP에서만 호출을 받습니다. Vercel은 IP가 매번 바뀌어서 직접 호출할 수 없어요. 그래서 고정 IP를 가진 Vultr 서버가 대신 키움을 부르고, Vercel은 그 서버를 부릅니다.

**Vercel 함수가 필요한 이유.** 대시보드는 https로 서비스되는데, 중계 서버 주소는 http입니다. 브라우저가 https 페이지에서 http를 부르면 보안상 차단해요. 그리고 야후 같은 외부 사이트도 브라우저가 직접 부르면 막힙니다(CORS). Vercel 서버가 대신 가져와서 전달하는 구조로 둘 다 우회합니다.

---

## 2. 매일 아침 할 일

**시가총액 파일 올리기.** 하루 한 번, 2~3분이면 끝납니다.

1. `KRX_수급비율_Colab` 노트북을 엽니다
2. **로그인 칸** 실행 — KRX 아이디·비밀번호 입력
3. **`marketcap.json` 저장 칸** 실행
4. 만들어진 `marketcap.json`을 GitHub 저장소 맨 바깥에 업로드
   (`Add file` → `Upload files` → 파일 끌어다 놓기 → `Commit changes`)

이걸 안 하면 대시보드가 어제 파일을 계속 씁니다. 당장 멈추지는 않지만, 그날 신규 상장한 종목이 수급 목록에서 빠지고 시총 값이 점점 낡아집니다.

**확인 방법.** 대시보드의 "시총대비 수급" 패널 오른쪽 위에 `시총 09.04 기준`처럼 날짜가 나옵니다. 어제나 오늘이면 정상이에요.

---

## 3. 화면에 있는 것들

| 패널 | 데이터 출처 | 갱신 |
|---|---|---|
| 국내 지수 (코스피·코스닥) | 키움 ka20005, ka20001, ka10051 | 30초 |
| 코스피200 야간 | Hyperliquid `xyz:KR200` | 15초 |
| SK하이닉스·삼성전자 | Hyperliquid + 업비트 환율 | 6초 |
| 아시아 증시·참고 지표 | 야후 파이낸스 (대만은 대만거래소, 미10년물은 CNBC) | 60초 |
| 시총대비 수급 | 키움 ka10065 + marketcap.json | 5분 |
| 순매수 상위 10 | 키움 ka90009 (평일 18:30 자동 수집) | 10분 |
| 뉴스 | 텔레그램 공개채널 `dada_news2` | 60초 |

---

## 4. 파일이 어디에 있나

### GitHub 저장소 (= Vercel에 배포되는 것)

```
index.html              화면 전체
flow-oscillator.html    수급오실레이터 페이지
marketcap.json          시가총액 (매일 아침 교체)
package.json            설정
api/
  market.js             업비트 환율 + Hyperliquid SKHX/SMSN
  macro.js              참고 지표 14개 숫자
  macro-spark.js        참고 지표 미니 그래프
  twse.js               대만 가권지수 (거래소 직접)
  kr200.js              코스피200 야간
  us10y.js              미10년물 (점검용, 화면 미사용)
  telegram-news.js      뉴스
  index-intraday.js     ─┐
  investor-rank.js       ├ Vultr 중계 서버를 부르는 창구
  supply-ratio.js        │
  flow-oscillator.js    ─┘
```

**⚠ 함수 개수 주의.** Vercel 무료 플랜은 `api` 폴더의 파일을 12개까지만 허용합니다. 지금 11개라 **하나 남았습니다.** 새 기능을 추가하기 전에 `us10y.js`를 지우거나(같은 기능이 `macro.js` 안에 있어서 없어도 됩니다), 여러 기능을 한 파일로 합쳐야 합니다.

### Vultr 서버 (`/root/app/`)

```
server.js               중계 서버 본체 (3000번 포트)
intraday.js             국내 지수 + 투자자 매매동향
supply.js               시총대비 수급
krx_investor_rank.js    순매수 상위 10 수집 (크론)
investor_rank.json      그 결과 파일
.env                    키움 앱키 — 절대 GitHub에 올리지 말 것
```

---

## 5. 문제가 생겼을 때

### 화면 일부가 "불러오지 못했습니다"

먼저 어느 단계에서 끊겼는지 봅니다. 브라우저에서 해당 주소를 직접 열어보세요.

```
https://night-dashboard-deploy-1.vercel.app/api/index-intraday
```

- 데이터가 나오면 → 브라우저 문제. `Ctrl+Shift+R`로 강제 새로고침
- `{"error":"..."}`가 나오면 → 그 메시지가 원인. 아래 참고
- `404 NOT_FOUND`가 나오면 → 파일이 `api` 폴더 밖에 있거나 이름이 틀림

### 국내 지수·수급이 안 나올 때

Vultr 콘솔에서 서버가 직접 뭐라고 하는지 확인합니다.

```
curl -s localhost:3000/index-intraday | head -c 300
```

`8005: Token` 같은 게 보이면 키움 토큰 문제인데, 지금은 자동으로 다시 받도록 되어 있어서 보통 저절로 풀립니다. 안 풀리면 재시작하세요.

```
pm2 restart all
```

### 서버가 아예 죽었는지 확인

```
pm2 list
```

`status`가 `online`이면 정상입니다. Vultr 콘솔 창을 닫아도 서버는 계속 돌아가요. 다만 Vultr 사이트에서 서버를 `Stop`하거나 `Destroy`하면 안 됩니다.

### 야후 지표가 통째로 멈췄을 때

야후가 호출을 막았을 수 있습니다. 잠시 기다렸다가 다시 보시고, 계속 안 되면 `index.html`의 `setInterval(fetchMacro, ...)` 값을 늘려서 호출을 줄이세요.

---

## 6. 알아두어야 할 한계

**전부 비공식 경로입니다.** 야후·CNBC·텔레그램·대만거래소 모두 문서화된 공개 API가 아니라, 각 사이트가 자기 화면을 그릴 때 쓰는 주소를 빌려 쓰는 방식이에요. 상대가 구조를 바꾸면 예고 없이 멈춥니다. 실제로 야후 전일 종가 문제로 여러 번 손봤습니다.

**코스피200 야간은 실제 KRX 선물이 아닙니다.** 해외 거래소(Hyperliquid)에서 형성된 24시간 계약 가격이에요. 거래량이 얕아 값이 튈 수 있습니다. 화면의 주의 문구를 지우지 마세요.

**시총대비 수급은 잠정치입니다.** 키움이 장중 몇 차례(대략 09:30, 10:00, 11:30, 13:20, 14:30) 집계하는 추정치이고, 집계 시각은 매일 달라집니다. 09:30에는 외국인만 나오므로 오전 10시 전에 다른 탭이 비어 있는 건 정상입니다. 마감 후 확정치와 다를 수 있어요.

**시세 재배포 문제.** 코스피·코스닥 지수, 투자자 매매동향, 시총대비 수급, 순매수 상위 10은 모두 KRX 시세를 키움 API로 받은 것입니다. 증권사 API 약관은 대체로 "본인의 투자 목적"으로만 이용을 허용하고 제3자 제공을 금지합니다. 유료 구독자에게 화면을 보여주는 건 별도 계약이 필요할 수 있어요. 키움 고객센터나 코스콤에 문의해두시는 것을 권합니다.

---

## 7. 수정할 때

**화면 문구나 숫자** — GitHub에서 `index.html`을 열고 연필 아이콘을 눌러 고친 뒤 `Commit changes`. 1~2분 뒤 자동 배포됩니다.

**참고 지표 종목 추가·삭제** — `api/macro.js` 맨 위 `INSTRUMENTS` 목록에서 한 줄을 넣거나 빼면 됩니다. 미니 그래프도 나오게 하려면 `api/macro-spark.js`의 `SYMBOLS`에도 같은 티커를 추가하세요.

**시총 하한선 변경** — Vultr 서버의 `supply.js`에서 `MIN_CAP` 값을 바꿉니다. 지금은 1,000억(`1000 * 1e8`)입니다.

**서버 파일 수정** — Vultr 콘솔에서 작업합니다. 모바일로는 실수하기 쉬우니 PC에서 하세요.
