# 배포 방법 (코딩 지식 없이 따라하기)

이 폴더에는 아래 파일이 있습니다.
- `index.html` : 실제 화면
- `api/market.js` : 시세 데이터를 대신 가져와주는 서버 (CORS 우회)
- `api/telegram-news.js` : 뉴스 패널용 텔레그램 채널 글 목록을 가져오는 서버
- `package.json` : 설정 파일

## 1. Vercel 가입
https://vercel.com 에서 GitHub 계정으로 무료 가입합니다.

## 2. 업로드
아래 둘 중 편한 방법으로 진행합니다.

**A) GitHub 없이 바로 올리기 (가장 쉬움)**
1. Vercel 대시보드에서 "Add New… → Project" 클릭
2. "Deploy without Git" 또는 이 폴더를 드래그 앤 드롭하는 화면이 나오면 이 폴더 전체를 올립니다
3. Framework Preset은 "Other"로 두고, Build Command는 비워둔 채 그대로 Deploy

**B) GitHub 연동 (나중에 계속 수정할 계획이면 추천)**
1. GitHub에 새 저장소를 만들고 이 폴더의 3개 파일/폴더를 업로드
2. Vercel에서 "Add New… → Project" → 방금 만든 저장소 선택 → Deploy

## 3. 확인
배포가 끝나면 `https://프로젝트이름.vercel.app` 같은 주소가 생깁니다.
그 주소로 들어가서 오른쪽 위에 LIVE 표시가 뜨는지 확인하세요.
(채팅 안에서 열어본 데모 버전과 달리, 서버를 거치기 때문에 LIVE로 뜰 가능성이 높습니다.)

## 4. 뉴스 패널

오른쪽 뉴스 패널은 텔레그램 채널(dada_news2)의 글 목록을 가져옵니다. 별도 API 키가 필요 없고, 자동으로 작동합니다.
다만 공식 API가 아니라 텔레그램 공개 페이지의 구조를 그대로 읽는 방식이라, 텔레그램이 페이지를 바꾸면 예고 없이 안 뜰 수 있습니다.
그럴 땐 `api/telegram-news.js` 파일을 다시 확인해야 하니, 안 뜬다는 게 확인되면 저한테 알려주세요.

## 5. 나중에 수정하고 싶을 때
`index.html` 안의 숫자나 문구는 메모장으로 열어서 직접 바꿔도 됩니다.
더 복잡한 수정(디자인 변경, 종목 추가 등)은 저한테 다시 요청하시면 파일을 새로 만들어드릴게요.
