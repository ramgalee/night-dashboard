# 배포 방법 (코딩 지식 없이 따라하기)

이 폴더에는 아래 파일이 있습니다.
- `index.html` : 실제 화면
- `api/market.js` : 시세 데이터를 대신 가져와주는 서버 (CORS 우회)
- `api/news.js` : 뉴스 데이터를 대신 가져와주는 서버
- `package.json` : 설정 파일

## 1. Vercel 가입
https://vercel.com 에서 GitHub 계정으로 무료 가입합니다.

## 2. 업로드
아래 둘 중 편한 방법으로 진행합니다.

**A) GitHub 없이 바로 올리기 (가장 쉬움)**
1. Vercel 대시보드에서 "Add New… → Project" 클릭
2. "Deploy without Git" 또는 이 폴더를 드래그 앤 드롭하는 화면이 나오면 이 폴더 전체를 올립니다
3. Framework Preset은 "Other"로 두고, Build Command는 비워둔 채 그대로 Deploy

**B) GitHub 연동 (나중에 계속 수정할 계획이면 추천)**
1. GitHub에 새 저장소를 만들고 이 폴더의 3개 파일/폴더를 업로드
2. Vercel에서 "Add New… → Project" → 방금 만든 저장소 선택 → Deploy

## 3. 확인
배포가 끝나면 `https://프로젝트이름.vercel.app` 같은 주소가 생깁니다.
그 주소로 들어가서 오른쪽 위에 LIVE 표시가 뜨는지 확인하세요.
(채팅 안에서 열어본 데모 버전과 달리, 서버를 거치기 때문에 LIVE로 뜰 가능성이 높습니다.)

## 4. 뉴스 API 연결하기 (오른쪽 뉴스 패널)

⚠️ 2026년 7월, 네이버가 뉴스 검색 API를 예전 개발자센터에서 **NAVER Cloud Platform(NCP)의 API HUB**로 옮겼습니다. 아래는 새 방식 기준입니다.

1. https://www.ncloud.com 에서 NAVER Cloud Platform 계정을 새로 만듭니다 (기존 네이버 아이디와는 별개의 가입 절차이며, 휴대폰 인증이 필요할 수 있습니다)
2. 로그인 후 콘솔 검색창에 "API HUB"를 입력해서 이동
3. 새 Application 생성 → 사용할 API에서 "Search API"(뉴스 검색 포함) 체크
4. 발급된 **X-NCP-APIGW-API-KEY-ID**와 **X-NCP-APIGW-API-KEY** 값을 복사
5. Vercel 프로젝트 화면 → Settings → Environment Variables 로 이동
6. 아래 두 개를 추가 (이름은 이전과 동일하게 유지했습니다):
   - `NAVER_CLIENT_ID` = 방금 복사한 X-NCP-APIGW-API-KEY-ID 값
   - `NAVER_CLIENT_SECRET` = 방금 복사한 X-NCP-APIGW-API-KEY 값
7. 저장 후, Deployments 탭에서 "Redeploy" 한 번 눌러주기 (환경변수는 재배포해야 적용됩니다)

이 값들은 코드 안에 직접 적지 않고 Vercel에만 보관되기 때문에, 압축파일이나 화면 소스코드를 통해 외부에 노출되지 않습니다.

무료 사용량은 통합 월 최대 77만 5천 건 정도로 넉넉해서, 개인 대시보드 용도로는 걱정하지 않으셔도 됩니다.

## 5. 텔레그램 뉴스 탭

오른쪽 뉴스 패널에 "텔레그램" 탭을 추가했습니다. 이건 별도 API 키가 필요 없고, 자동으로 작동합니다.
다만 공식 API가 아니라 텔레그램 공개 페이지의 구조를 그대로 읽는 방식이라, 텔레그램이 페이지를 바꾸면 예고 없이 안 뜰 수 있습니다.
그럴 땐 `api/telegram-news.js` 파일을 다시 확인해야 하니, 안 뜬다는 게 확인되면 저한테 알려주세요.

## 6. 나중에 수정하고 싶을 때
`index.html` 안의 숫자나 문구는 메모장으로 열어서 직접 바꿔도 됩니다.
더 복잡한 수정(디자인 변경, 종목 추가 등)은 저한테 다시 요청하시면 파일을 새로 만들어드릴게요.
